FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy the rest of the application
COPY . .

# Expose port
EXPOSE 3000

# Default command with explicit host binding
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"] 