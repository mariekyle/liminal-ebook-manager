> Append-top. Never edit old entries. If a decision changes, add a new entry that names what it supersedes.

---

# How to Use This File

- **Format (from 2026-09-23, D-001 onward):** one block per decision, newest first, under `# Log`. The heading carries the decision itself, not the topic, so search returns the answer:

  ```
  ## D-NNN · YYYY-MM-DD · <the decision>
  Why: <one or two lines>
  Rules out: <what this closes off>
  Supersedes: D-NNN   (only if it does)
  ```

- **Numbering:** D-NNN counts up from D-001. Entries before 2026-09-23 keep their original dated-sprint format under `# Log (before D-001)` and are cited by date, as they always were. Nothing below that line is ever renumbered or rewritten.
- **On the phone:** After working through a question in the Claude Project, add the decision here as a D-block. Don't write an essay.
- **At the laptop:** Reference this before implementing. If you can't find a decision for what you're about to build, that's an Open Question — add it there, don't just wing it.
- **When a decision changes:** Don't delete the old one. Add a new block with `Supersedes:`.

## Pending ratification

Decisions Claude Code made because the docs were silent. Marie ratifies by saying so in a
session; Claude Code then moves the block below the line. Newest first.

_Nothing pending. D-001 and D-002 ratified by Marie, 2026-09-24._

---

# Log

## D-013 · 2026-09-24 · The Beelink serves Liminal over HTTPS via `tailscale serve`, the same as todo, and this is cutover precondition 8; the phone installs it as a PWA from that origin
Why: PWA install and service-worker registration require a secure context. Today's origin is plain HTTP on the LAN, which is why Liminal has never been installable. `tailscale serve` terminates TLS with a tailnet cert, no port opened, no certificate to manage; the phone already runs Tailscale for todo. Repointing to the new origin resets localStorage (view mode, sort); accepted. Recon at prompt time: confirm `frontend/` ships a web manifest and a service worker; if not, adding them is in the cutover session's scope, not a later sprint.
Rules out: A plain-HTTP cutover. LAN-IP bookmarks as the primary access path after step 8. Any inbound port.

## D-012 · 2026-09-24 · The tracked `docker-compose.yml` is the Beelink run file; every host path is a required env var, and the real values live only in the TrueNAS app config and `CLAUDE.local.md`
Why: TrueNAS Apps holds the YAML with real paths; the repo holds the same shape with `${DATA_HOST_PATH:?}`, `${BOOKS_HOST_PATH:?}`, `${BACKUPS_HOST_PATH:?}` so it fails loudly when unset and never carries a path. Both `BOOKS_PATH` and `BOOKS_DIR` are set to `/books` (upload.py reads the second name). `.dockerignore` excludes `CLAUDE*.md`, `data/`, `*.db`, `.env`, `docs/`, `scripts/`, `frontend/node_modules`.
Rules out: A build-mode compose in the repo. A second compose file. Any host path in git.

## D-011 · 2026-09-24 · Cutover runs in the order Claude Code laid out, amended: `scripts/lcheck.sh` and the SMB deploy section are deleted in the retirement commit (step 9), not step 1; the NAS `data/` folder is the rollback for 30 days; the prototype's volumes are deleted after one look at its Postgres
Why: Between step 1 and step 9 the NAS is still production; the rulebook describes reality at every commit or it's not a rulebook. The prototype's Postgres is the last consumer of the leaked June-2025 passphrase; deleting its volumes closes phase 1 for good. Preconditions 1–7 run on a throwaway container against a copy of `library.db` before anything touches the real one; the day-long phone test between steps 8 and 9 is the only window where a rollback loses data, so nothing irreplaceable gets added during it.
Rules out: Running the NAS and Beelink apps in parallel against the same data (no shared-writer SQLite, ever). Rewriting CLAUDE.md's deploy section before the Beelink is production.

## D-010 · 2026-09-24 · `main.py`'s `version=` stays the version of record; the release paste block tags `vX.Y.Z` to match, and Publish fails if the tag and `main.py` disagree
Why: A build arg would leave local dev and the M1 versionless and lets the tag and code drift the other way. One source in the repo, one CI assertion, zero drift.
Rules out: Build-arg version injection. The git tag as sole source. Publishing an image whose `/openapi.json` disagrees with its tag.

## D-009 · 2026-09-24 · `main.py`'s lifespan snapshots `library.db` through the backup service before `init_db()` runs; skipped when no DB file exists; a snapshot failure aborts startup
Why: Promote removes the human who used to back up before schema changes. Putting the guard in the process that runs the migrations makes it deploy-mechanism-agnostic. A container that refuses to start is visible; a migration with no backup is not. Filename shape `liminal_pre_deploy_YYYYMMDD_HHMMSS.db` so the v0.67.0 retention parser ages them out.
Rules out: Relying on the deploy mechanism, the scheduler, or Marie for the pre-migration copy. Fail-open on snapshot error.
Rider (2026-09-24, ratified at implementation): `create_backup` never raises; it returns `{"status": "failed"}`. So `main.py` checks the returned status and raises on `"failed"`; an `except` around the call would fail open. `"skipped"` (backups disabled in Settings) is not a failure: startup continues with a warning that says explicitly that backups are disabled and no pre-deploy snapshot was taken.

## D-008 · 2026-09-24 · A sentinel file at the books root distinguishes "library unmounted" from "library empty"; sync refuses and `/api/health` reports the library unreachable when it is missing
Why: An unmounted NFS share is a mounted, empty directory. Sync never deletes, but it would orphan every title and un-orphan them on the next run — pure churn that also poisons the sync-results view. Path existence can't tell the two apart; a marker file (`.liminal-library`) can. Created once by hand on the NAS.
Rules out: Emptiness heuristics. Any sync run against a root without the marker.

## D-007 · 2026-09-24 · The Synology NFS export for the Beelink maps root to admin ("Map root to admin"), not "No mapping"; a throwaway-container write test is precondition 2
Why: The image runs as root; with root_squash on, every write lands as `nobody` and upload fails with EACCES on the first real use. "Map root to admin" gives writable files owned by the NAS admin; "No mapping" is strictly more permissive for no gain. Test: `docker run --rm -v <books mount>:/books alpine sh -c 'touch /books/.w && rm /books/.w'`.
Rules out: Running the container as a non-root user to dodge the mapping (revisit only with a reason). Discovering the squash setting at first upload.

## D-006 · 2026-09-24 · Data placement: `library.db` and `covers/` on the Beelink's local ZFS (`apps_pool`); `/books` is the existing NFS mount of the NAS Media share, writable; backups write to an NFS folder on that share (`Reading/_liminal-backups`) via the app's `backup_path` setting
Why: SQLite over NFS corrupts under lock contention — the one thing in this plan that can destroy data. The Beelink already mounts the Media share; no second export needed. Backups as whole-file copies are fine over NFS, and this puts the DB's history on a different box than the live file — a better split than today.
Rules out: SQLite on any network filesystem, ever. A second NFS export. Backups on the same disk as the live DB.

## D-005 · 2026-09-24 · Deploy pipeline is todo's: Publish on tag builds `linux/amd64` and pushes to `ghcr.io/mariekyle/liminal-ebook-manager`; manual "Promote to stable"; the Beelink pulls `:stable` on a manual TrueNAS Apps update
Why: The build happens on the runner, so a Rollup failure is a red X before anything touches the Beelink. Promote is the staging gate the push-to-deploy webhook never had. The pull is a click in a web UI Marie already uses. `promote.yml` verbatim; `publish.yml` adapted — the smoke step runs the image and asserts HTTP 200 on `/api/health` (not the string "healthy", which needs a books path). The GHCR package is set public after the first push.
Rules out: Watchtower or any automatic pull. Building on the NAS or the Beelink. Any webhook.

## D-004 · 2026-09-24 · The `liminal_*` prototype on the Beelink (June-2025 Postgres/Redis stack) is stopped, its ports freed, and its containers and volumes removed as step 2 of the cutover
Why: It squats on 3000/8000, has never served the library, and its Postgres is the only remaining consumer of the leaked passphrase. Marie takes one look at the Postgres contents before the volumes go, in case of a surprise. There won't be one.
Rules out: Keeping it "just in case." Reusing its compose, images, or storage folder for the real app.

## D-003 · 2026-09-24 · Production moves from the Synology (Container Manager, SMB copy) to the Beelink, running the registry image; the Synology keeps the books, the backups, and a 30-day cold copy of the old data folder
Why: The SMB copy is the step that failed silently twice (v0.55.0); `lcheck.sh` narrows it, a registry image removes it. Rollback becomes "promote the previous tag." The Beelink is the box built to run apps; the NAS is the box built to hold files.
Rules out: The SMB-copy + Container Manager path after step 9. Any deploy that involves copying source files to a host.

## D-002 · 2026-09-23 · DECISIONS.md switches to numbered `## D-NNN · date · decision` blocks with a Pending-ratification section; earlier entries keep their format
Why: Same shape as the todo repo, so one workflow serves both. The old dated-sprint entries are the record of how the sprints were decided; converting them would rewrite history for no gain.
Rules out: Renumbering or reformatting anything under `# Log (before D-001)`. A second decisions file.

## D-001 · 2026-09-23 · A commit that changes app code must also change the project record: anything under `docs/`, or `CHANGELOG.md` or `ROADMAP.md` at the root; `docs/DESIGN_LINT_REPORT.md` alone doesn't count
Why: The pre-commit hook in `.githooks/` enforces it. The lint report is refreshed and staged by the hook itself, so it would satisfy the rule on every commit without anyone writing anything; it is excluded so the rule means a human-written change. `CHANGELOG.md` and `ROADMAP.md` stay at the root because README, ARCHITECTURE, .cursorrules and the Liminal skill all cite them there.
Rules out: Moving CHANGELOG.md or ROADMAP.md into docs/. Requiring one specific file per commit (todo's D-050 requires PIPELINE.md; Liminal's PIPELINE.md is a queue, not a per-commit status file).


# Log (before D-001)

> Dated-sprint format, as written at the time. Cite by date.

#### **==2026-09-03 — Implementation sequencing after the UI Mockup Sprint==**
Ratified: Marie, 2026-09-03. Supersedes the post-v0.87.0 Pipeline queue order (#2–#4) and ROADMAP's 10.3 "on hold." Context: no code has shipped since v0.87.0 (2026-07-26); August was design-only.

- **SEQ1 — Mockup sprint closes first.** Slice 7 capture → Slice 8 audit close. No implementation prompts until Slice 8 is captured. Reasoning: a sprint at 87% is a sprint that never ends.
- **SEQ2 — Slice 10 (Add flow) pulled forward, mocked WITH the 10.3 lookup states** (search, results, prefill, no-match, error). Reasoning: 10.3's UI lives on the Add flow; designing it once beats building a throwaway panel and re-skinning it later. Slices 9–10 were parked "until v0.86–0.87 code settles" — it has been static five weeks; the parking reason is dead.
- **SEQ3 — 10.3 External Book Search is the FIRST implementation block**, immediately after Slice 10 capture. Decision sprint → prompt batches. Reasoning: highest-frequency daily pain (manual metadata entry per wishlist add), 2–3 sessions, zero dependency on nav or any redesigned screen; a short win before a long implementation is the momentum play.
- **SEQ4 — UI implementation runs in blocks. Foundation block first:** nav IA (NAV1–7, touches every screen) + Button bordered variant + light-token ratification outputs. Per-screen block order is NOT locked here — it gets its own decision sprint after recon against the post-Foundation tree.
- **SEQ5 — Search Expansion merges into the Search screen block.** Reasoning: LIB13/SRCH8/SRCH14 assume authors, notes, and wishlist are searchable. It is a dependency of the screen, not a side item; building the UI against today's backend renders results that cannot be returned.
- **SEQ6 — Button bordered-variant decision moves to the Foundation block.** Reasoning: a shared-component gap every mockup with a bordered secondary action depends on — not a DuplicatesPage cosmetic.
- **SEQ7 — 10.1b (+ date_added rider, 2026-08-02) floats.** Pure backend, no UI dependency; interleave between UI blocks at Marie's discretion. Not gated, not gating.
- **SEQ8 — Slice 9 (DuplicatesPage restyle) trails everything.** Page is at zero real duplicates and opened rarely; its one defect is a missing button border that SEQ6 fixes anyway.
- **SEQ9 — Defect policy.** Standalone defects (add-flow title persistence, wishlist author link → error page, Notes section on TBR detail) = one unscheduled fix session. Every other backlog defect either dies with the redesign of its screen or is absorbed by 10.1b / Search Expansion. Recorded so that waiting is a decision, not neglect.
- **SEQ10 — Open Questions triage rides Slice 8's audit close.** Split into Defects / Wants (~25 items are feature wishes filed as bugs). Not before Slice 8: half the defect list dies there and reorganizing twice is stalling with extra steps.

#### ==2026-08-23 — Home-Purpose Decision Sprint (unblocks Slice 7)==

Mockup: liminal-home-mockup-v8.html (canonical; home v5 superseded, v6–v7 discarded). Resolves the 2026-07-30 Home-purpose and third-track open questions.

**HP1 — Home purpose: the reading desk — what I'm in, what I'll pick.** Two facets, one job: track active reading, explore what to read next. Launcher REJECTED: the bottom nav is the launcher; a second one is NAV7's junk drawer in a nicer coat, and the usage ranking never included Home as a hub. Pulse-as-dashboard REJECTED: stats have their own room (H5 is a doorbell, not a readout). Liminal tracks reading, it doesn't host it — Home surfaces the ledger's living edge, not a resume button.

**HP2 — Home = two sections (amends H2): Currently Reading + Explore.** Recently Added leaves Home — Library owns "newest." H5 (conditional doorbell/greeting), H6 (no captions — except HP3's override), H7 (header) unchanged.

**HP3 — Currently Reading is a rail of row cards, uncapped.** Retires H3 (deck contract) and H4 (third track — the question dissolves with the deck row). Anatomy per card: 56×84 cover thumb (art only, no overlaid text) with H3's surviving 3px progress bar beneath; meta column matches the thumb's height and anchors to its foot — title (2-line clamp) above author above "~Nh left." [Override log: Marie overrode H6 for this section after phone review — captions return here because the row IS the caption; full-cover tiles read as no breathing room. H6 stands everywhere else on Home.] Resolves the "currently reading should have no limit" open item. Reading-empty renders an in-section whisper (not a STATE block — the library isn't empty): "Nothing open right now. Pick something below." NEW STRING.

**HP4 — Explore = lens rails.** A lens is a query Liminal can already answer, rendered as a rail: 8 covers of the one 112px cover module (LIB11, H6 capless), lens head = small-caps label (S1 scaffolding) + plain secondary subline carrying the named content ("FROM YOUR SHELVES · Comfort Rereads · 11 unread"). 2–3 lenses per load; shuffle swaps the lens set, not just the covers. Replaces Discover Something New and absorbs Quick Reads (good lens, bad section).

**HP5 — Lens set v1 (all answerable by current schema):** Next in Series (owned unread, number = last finished + 1) · From Your Shelves (a manual collection's unread, subline names it) · More From (author of most recent finish) · Been Waiting (old unread by date_added) · Just Enough Time (length tier) · Same Ship / Same Fandom (fanfic metadata, after last finish). The library-database connect adds lenses later; the structure doesn't change.

**HP6 — Zero-result lenses never render.** No header, no empty rail, no placeholder — AUTH3's silent degradation doing app-wide work. Shuffle draws only from lenses with results. Explore degrades to whatever the library can answer; floor = Been Waiting + Just Enough Time (need only date_added and word count), so any GitHub stranger's library qualifies and a fanfic-less library never learns the ship lens exists.

**HP7 — Lenses draw RANDOMLY from their full match set,** reshuffled per load and per shuffle tap. Sort-then-slice is the NAMED ANTI-PATTERN — it is exactly why the live Quick Reads rail fossilized (same top-8 forever). Been Waiting draws randomly from the oldest quartile, not the strict 8 oldest. Just Enough Time rotates which ratified length tier it wears per appearance (subline carries the tier). Next in Series and More From stay deterministic — small, specific match sets where randomness is noise.

**HP8 — The tally.** Foot of the page, quiet surface card (S7: it's a data group), two-column grid: text column + vertically centered chevron. Small-caps period label → 40px Georgia tabular-nums number with inline small "words" → whisper comparison ON ITS OWN LINE translating the number into a book from the reader's own library ("The whole Wheel of Time, and change."), rotating per visit. Period = THIS YEAR: the doorbell owns the month, so the tally is a different, bigger, ever-climbing number — the scroll-down reward. Tap → Stats. The comparison is a mechanism (word-count-match query + template), flagged as feature scope, not pure design.

**HP9 — Rail mechanics.** scroll-padding-left = gutter (without it, scroll-snap eats the left gutter on load — the v6 bug); partial 4th item peeks to signal scroll.

**HP10 — Stats content mandate (Slice 7's brief).** Home promises the doorbell (month: words · time · finished) and the tally (year: words + comparison); Stats is the ledger behind both, over the ratified period selector. Content: Volume (words, hours) · Finishes (count AND the list — LIB9 rows, newest first; the number is a door, the list is the room) · Rhythm (finished-per-month, the only chart, calm bars) · Set Aside (DNF count, neutral tone, silent degradation). FORBIDDEN, per AUTH1/SER5: goals, streaks, % of library read, unread counts, pace-vs-last-period arrows. Stats records what you did, never what you didn't.

#### ==**2026-08-20 — UI Mockup Sprint Slice 6 · Series & Author Detail**==
Both pages are pushed routes serving three inbound paths: Library scopes (LIB12/LIB13), Search scoped results (SRCH13), and Book Detail tappable metadata (S2). Back target and lit nav tab follow the returnUrl origin.
###### **Cross-page**

**SA1 — Series byline derivation (app-wide data rule).** If all owned titles in a series share one author, that author is the series byline. If authors are mixed, the byline is "_[most-frequent author]_ & others" — one derivation everywhere a series byline renders (Library series rows, Search series results, series detail header). On the series detail header only, the byline drops entirely when the author name equals the series name (AO3 self-named series; "Alliefaire by Alliefaire" is stutter, not information). _Retro-touches LIB12 and SRCH8 rendering — flagged for the Slice 8 audit._

**SA2 — Bylines are deviation signals on homogeneous pages (ratified LIB9 deviation).** LIB9's "author is always row 2" guards heterogeneous lists where author differentiates. On series and author pages the population is homogeneous by construction, so the byline renders **only when it deviates** from the page's byline: full stored byline on series rows whose authors differ from the header (Dune 07–08), "with _[Author]_" on author-page titles with co-authors. Silence = expected author. Same silence-is-neutral logic as LIB10, same divergence license as SRCH9.

**SA3 — Notes stay on Book Detail.** Series and author pages render no notes section, ever — no content, no empty state. SRCH14 note hits deep-link to the title's Notes tab. The "No notes yet" billboard is not killed here; it is never built.

**SA4 — Route-miss state.** Deleting a series/author's last title evaporates the page; stale links land on STATE2 error anatomy in plain voice: "Not in your library anymore." + ghost **[Back to Library]**. One state, both pages. Top bar keeps back + type label, no kebab (no actions on nothing).

**SA5 — Pushed-route top bar.** Book Detail v5's sticky glass bar verbatim (52px, teal back with origin label, 40px kebab) **plus a centered small-caps screen-type label** ("SERIES" / "AUTHOR") — scaffolding per S1, and genuine wayfinding: identical names exist as both series and author (Alliefaire). Kebab opens a COLL11 glass sheet; **Edit** is the one known action per page (full menu contents: recon at implementation). On the author shrine, the bar is **transparent at rest** — the ambient wash runs from pixel zero — and the glass returns on scroll (~28px). Glass is the scrolled state, not the resting state; S4 holds.

###### **Series detail**
**SER1 — Hero anatomy.** BD1-shaped: 112px square monogram gradient tile left (LIB12 identity language; future multi-title series render a 2×2 cover mosaic here — MosaicCover language, display-only reuse, frozen file untouched), then title → conditional byline (SA1) → stats line. No banner, no controls, straight into rows. The wrapping card and its "Books in Series" head are dead (S1/S7).

**SER2 — STATUS COLOR LIVES IN MARKS, NEVER IN WORDS.** The stats line ("6 titles · 4 finished") is plain secondary text — no tint, no pill. Status color renders only in marks: BD9's sage row checks. Tinted stat _text_ is fenced to the completion domain (COLL14 checklist stats keep theirs; COLL8's wall stands). Everywhere else, colored text means tappable (S2) — which is exactly why a tinted count misreads as action.

**SER2b — Byline link anatomy.** "by" renders plain, the **name** is the teal link → author page, and any "& others" suffix renders plain. Only the person is tappable; Dune resolves to Frank Herbert's page and nothing pretends "others" is a destination. (Matches BD v5 hero byline.)

**SER3 — Rows = BD9 promoted to page scale.** Anatomy: tabular number rail (decimals supported) · title · status. Number and status **top-align with the title's first line**. Titles clamp at two lines (ratified LIB9 deviation: shared-prefix titles are the norm in a series; single-line ellipsis eats the only words that differ). Stars sit **below the title** (rated-only, filled-only; BD11's partial-rating question stays open). Deviation byline (SA2) renders as a sub-line. DNF renders as its text label, neutral tone, via `useStatusLabels`. Rows sit on base with LIB9 dividers (SRCH9 long-list rule).

**SER4 — Series detail has no controls.** No sort (the number **is** the order — COLL7's asymmetry, extended), no layout toggle (a cover grid deletes the number rail, which is the page's job), no search field (the page is a record to view, not a corpus to query — holds even for long series).

**SER5 — Numbering gaps render silently.** Owned entries list with their true numbers; gaps speak for themselves. No ghost rows (Liminal doesn't know the full roster pre-Liminal-Connects), no missing-counts — that's a guilt metric.

**SER6 — Edge stats.** Singular: "1 title · finished". All read: "_N_ titles · all finished". One-title series render the normal page — one row is a legitimate room. No whisper layer on series pages (S5 permits zero).
###### **Author detail — the Shrine**

**AUTH1 — Billboard.** Standard H1 (one heading register on every page — no serif exception), 34px air above, ambient count in the stats line, bio below, 26px air beneath the block. Edit lives under the kebab (SA5) — no inline pencil. **No reader-progress stat on author pages**: "2 of 47 read" is guilt, not progress; authors are open corpora, series are bounded sequences (principled asymmetry with SER2's finished count).

**AUTH2 — Shrine structure ratified** (supersedes the section-grouped draft). Page order: top bar → billboard (name · stats · bio) over the ambient wash → SERIES rail → TITLES row → flat grid/list. The page is organized around the author's output, not the reader's filing.

**AUTH3 — Accomplishment stats + SILENT DEGRADATION.** Stats line celebrates the author: "_N_ titles · _N_ series · _N_ words". Segments render when data exists and **vanish without placeholder when it doesn't** ("29 titles · 4 series" for word-count-less corpora, "1 title" for a single standalone). The rule generalizes to any stat segment, app-wide.

**AUTH4 — Ambient palette wash.** A top gradient from the author's dominant cover-lane color at .26 alpha, fading to base over ~340px, behind bar and billboard. Derivation reads GradientCover palette data — display-only, frozen files untouched. The room takes the author's colors on the same theory that lets cover art carry color as content (S8). Light-theme alpha gets its own value at the light-token pass (open question).

**AUTH5 — Series rail.** Series render as **objects**: horizontal rail of 112px LIB12 square gradient tiles, count badge top-right, name below in a reserved 2-line box, tiles top-aligned, tap → series detail. Rail label is small-caps "SERIES · _N_" (scaffolding); series names themselves are named content → Title Case (S1 applied precisely; the live app's "Series:" prefixes are dead).

**AUTH6 — TITLES row + sort.** Small-caps "TITLES · _N_" left; controls right: LIB8 sort trigger → ratified glass sheet with direction hints, options **Title / Recently added / Recently published** — "Series order" does not exist as a sort; the rail owns grouping. LIB2 two-icon layout pill (grid/list). No search field within one author.

**AUTH7 — Content anatomy.** Grid cells: LIB11 indicator zone — number badge top-left, monochrome status mark top-right (LIB10); covers untouched, including in-art author names (cover art is content, S8 — same as a printed cover). List rows: LIB9 anatomy with **row 2 = placement, not identity** ("_Series_ #NN" · "with _[Author]_" · silence); right meta = read time + toned status label.

**AUTH8 — One-title author.** Controls are removed, not hidden (LIB4 semantics). Billboard + rail/grid of one stands alone. Empty bio renders nothing — no placeholder.

**Register note (S5 accounting).** With no aggregate whisper on either page, the author's own bio — rendered in whisper register (Georgia italic, muted) — is the shrine's one whisper. User-authored text entering whisper register is hereby a sanctioned register, not ad-hoc styling.
###### **Strings → MICROCOPY ledger (flagged, unratified until the verification pass)**
- "_[N]_ titles" — ambient count (the "in your library" suffix is retired from ambient contexts; it remains transactional register)
- "_[N]_ titles · _[M]_ finished" / "1 title · finished" / "_[N]_ titles · all finished"
- "_[N]_ titles · _[N]_ series · _[N]_ words" (+ silent-degradation forms)
- "_[Name]_ & others"
- "with _[Name]_"
- "Not in your library anymore." + [Back to Library]
#### ==**2026-08-05 — UI Mockup Sprint · Slice 5: Collections**==
Mockup: liminal-collections-mockup-v2.html (canonical; v1 discarded).
- **COLL1 — Index anatomy.** Grid = custom-art tiles, name only — no counts on tiles (counts live in list meta and detail stats, where they're information). List = LIB9 rows on base; the live app's per-row card mats die. Index is FLAT: no Default/My sectioning — the distinction is functional only where behavior differs, so the section split survives solely in reorder mode (explains un-draggable rows) and edit contexts (explains undeletable collections). [Ratified as an override of the mockup-v1 always-sectioned proposal.]
- **COLL2 — Index whisper count line.** "57 collections" plain voice becomes a whisper line matching Library/Search. Mockup placeholder: "57 shelves, each with a reason." — NEW STRING, MICROCOPY_LIBRARY verification before ship.
- **COLL3 — Create surfaced.** + New collection is a header icon action on the index; the kebab keeps Reorder. Creation is a primary act and doesn't live two taps deep in a bottom sheet.
- **COLL4 — One detail template, variant-aware.** Manual, checklist, and auto are the same screen with variant slots, not three designs. Header mirrors Book Detail anatomy: banner → title + type chip (chip absent for manual — absence is the default) → plain stats line → variant whisper → user description (their voice, body text) → controls row (search field + Grid/List + sort icon when applicable) → content.
- **COLL5 — Variant notices are whispers, not card mats.** The auto notice paragraph-card and the checklist hint-card die. Auto: "Tends itself, based on your reading." Checklist: "Long-press a book to mark it complete." One line, serif italic, muted. Both NEW STRINGS for MICROCOPY verification.
- **COLL6 — Completed sub-section pattern.** Small-caps section label ("Completed · N", S1 language) + dimmed rows/covers. Card mat removed. Capped section → no internal dividers (SRCH9 length rule). Pattern defined once; checklist is its only current tenant — the "inconsistency" was a pattern that had never been specified, now it is.
- **COLL7 — Sort is icon-only, auto collections only.** Sort-lines-with-arrow icon in the controls row (sortline row deleted); opens the ratified sort sheet. NNG heuristic-1 mitigation: when sort ≠ the collection default, the icon takes the active teal tint (Search funnel-badge language) so non-default state stays visible. Manual/checklist have NO sort — hand order IS the sort; the asymmetry is principled.
- **COLL8 — Two checkmark semantics, deliberately split.** Reading-status marks stay neutral (LIB10). Checklist-completion badges use the success tint. Different facts, different tokens, captured so the audit reads it as intent.
- **COLL9 — Empty states are variant-aware.** Whisper + action per STATE1/2/4; emoji retired. Manual: "An empty shelf, ready for whatever arrives." + [Add books]. Checklist: "A checklist with nothing to check. Yet." + [Add books]. Auto: "The rules are set. Nothing matches them yet." + [Edit rules]. All NEW STRINGS for MICROCOPY verification.
- **COLL10 — Kebab menu is a two-axis matrix (verified in-app 2026-08-05).** Composed, not variant special-cased: default collection → no Delete Collection; auto collection → no Remove Books / Reorder Books (membership is rule-driven). Verified by duplicating Reading History as a user auto: menu showed Edit / Duplicate / Delete exactly as the matrix predicts. Live app already behaves correctly; this captures it as spec.
- **COLL11 — Collection modals are canonical glass bottom sheets.** Create / Edit / Duplicate use Library v5's sheet anatomy verbatim: scrim, grab handle, small-caps sheet title, and a sheet-actions footer — quiet Cancel text left, filled teal primary right (Create / Save / Duplicate, 46px, radius-md). No full-screen dialogs, no top-right text actions, no X-close. ONE sheet language app-wide; the tag sheet's text input is the precedent that forms belong in sheets.
- **COLL12 — Modal content rules.** Type cards use stroke icons (emoji retired) with a one-line note that follows selection. Edit: type shown as a locked row with quiet "can't be changed"; rules (auto) shown read-only with "Rules can't be changed after creation — duplicate to start from these." Duplicate is type-reactive: Automatic shows the rules grid + live "~N books match" + "N rules active"; Manual/Checklist shows "Books will be copied as they are now." Create reveals an EMPTY rules grid when Automatic is selected — all fields at Any, "0 rules active," match count reading the full library ("2,214 books match") as honest feedback that nothing is constrained yet. Modal helper lines are NEW STRINGS for MICROCOPY verification.
- **COLL13 — Red diet.** Destructive fill is reserved for the Delete Collection confirmation. "Remove custom cover" and "Clear all" (rules) are quiet destructive text links. Red is a budget, not a mood.
- **COLL14 — Checklist stats line.** "N books · M complete" with the complete count in success tint — progress lives in the stats line, not a separate widget.
#### ==**2026-08-03 — UI Mockup Sprint · Slice 4: Search (absorbs Browse)**==
- Mockup: liminal-search-mockup-v3.html (canonical; v1–v2 discarded). Nav icon swap applied to liminal-home-mockup-v5.html and liminal-library-mockup-v5.html (replaced in place).
- **SRCH1 — Search is a tab, not a modal.** NAV4 realized: results live in Search itself, bottom nav present, no close X, no handoff to another screen. The old modal's "Filter library by X →" escape hatch dies; its idea (query as composable filter state) is internalized.
- **SRCH2 — The input is the header.** Sticky glass zone = search field + funnel button, nothing else. No "Search" title row — the placeholder is the screen's name. Placeholder: "Search your library..." (ratified, VOICE_AND_TONE).
- **SRCH3 — No autofocus on tab entry.** The keyboard must not bury the discovery layer on arrival. Tap the field to type. This is the Browse-absorption argument settled at the interaction level.
- **SRCH4 — Live debounced search.** ~250ms debounce, minimum 2 characters, no submit step. Enter only dismisses the keyboard. Query and filter chips compose freely.
- **SRCH5 — Pre-type state = recents + browse-by.** Two layers: Recent searches (≤5, removable rows, section-level Clear, no timestamps) above Browse-by pill groups — Length (the 7 ratified poetic tiers), Category, Status — in LIB6 pill language. One whisper for the Browse context: "No destination needed." (S5: one whisper per context). No trending, no suggestions, no algorithmic anything.
- **SRCH6 — Discover pills are one-tap doorways.** Tap a pill → immediately in results with that chip active. No multi-select, no apply step. Single tap, single chip; refinement happens after entry.
- **SRCH7 — Refinement = the LIB6 filter sheet via a funnel button.** 44px funnel beside the input (active-count badge, teal when filters live) opens Library's filter sheet verbatim — no second filter UI. Active chips render in Library's chip row language (removable chips + Clear all).
- **SRCH8 — Grouped results: Titles → Series → Authors → Notes.** All-view = grouped preview: small-caps group labels with counts (S1), groups capped at 4 rows + a quiet borderless "All N titles →" link row. Titles = LIB9 rows; series = LIB12 40px square thumbs; authors = LIB13 arow (name + count). Order matches the Library scope control's mental order, Notes last. LIB13's hard requirement (Search owns author search) is discharged here.
- **SRCH9 — Divider rule follows list length, not screen.** Within capped preview groups: no dividers — padding separates rows; one hairline closes each group; the last group closes with nothing; the "All N" row is borderless. In scoped flat views (SRCH13): LIB9 dividers RETURN — long homogeneous lists are exactly what dividers were ratified for. Recents keep their dividers (uniform utility list with adjacent remove targets). Deliberate, principled divergence — Slice 8's audit should treat it as ratified, not drift.
- **SRCH10 — Chips-only browse shows Titles only, no scope tabs.** Book filters filter books (LIB4 logic); Series/Authors/Notes groups and the scope tab row are suppressed when browsing by chips with an empty query — one implicit scope. Ambient count whisper uses the ratified per-tier MICROCOPY phrases ("212 quick escapes. Just enough time.").
- **SRCH11 — No-results split by cause, ratified strings only.** Filters active → "Nothing matches all of that. Loosen a filter or two." + [Clear all filters] (STATE4 verbatim). Query only → "Nothing found — try different words?" in whisper, no action (MICROCOPY verbatim). Chrome + chips stay visible per STATE3. No new microcopy strings for these states.
- **SRCH12 — Nav iconography ratified app-wide.** Collections = stacked-shelves mark (two full rows + shorter third), adopted into Home and Library canonical mockups. Library tab = italic L glyph (Georgia serif italic) everywhere. Nav icon strokes = 1.75. Ratified choice, not accident — audit accordingly.
- **SRCH13 — Result scope tabs (federated pattern).** When a query is live, a sticky glass tab row (BD6 tab language) renders under the header: All · Titles · Series · Authors · Notes, each with a count. All = default grouped preview; a scope tab = flat LIB9 list with dividers and a scoped whisper line; query + funnel stay live above. "All N titles →" and the Titles tab are the same destination — one mechanism, two entry points; back = tap All (the tab row is the wayfinding, no separate back affordance). Zero-count tabs render dimmed, never hidden (stable layout, teaches the scopes). NNG basis: All-default federated results with counted scope tabs is the standard; the anti-patterns are pre-scoped search and hidden cross-scope results. Tab counts surface buried groups — the Notes-burial concern is answered in chrome, before any scrolling.
- **SRCH14 — Notes result anatomy.** Notes are a separate fourth group, never folded into Titles: a title matched by its name and a title matched by your note about it are different findings — mixing them corrupts counts and scatters snippets. Row = LIB9 title row + a third snippet line: matched text, muted, single-line ellipsis, match term stepped up to body color at 500 weight — no bold shouting. Scope of WHICH note types are searchable is NOT decided here — that belongs to the Search Expansion sprint's recon (recon before decisions).

#### **==2026-08-02 — date_added semantic on wishlist conversion==**
- **Decision:** When a wishlist entry converts to an owned title (Acquire/link, add-to-existing, Move to library), `date_added` is set to the conversion date. The wishlist-added date is not preserved anywhere.
- **Reasoning:** date_added answers "when did this enter my library" — a wishlist entry isn't in the library yet. Wishlist tenure is not a stat worth a column. Ratified: Marie, 2026-08-02.
- **Scope:** Forward behavior only. Existing converted rows keep whatever date_added they have — no retroactive rewrite.
- **Implementation:** Rider on the 10.1b non-EPUB extraction sprint — same conversion code paths as the ratified auto-scan. No standalone session.

#### **==2026-08-02 — UI Mockup Sprint · Slice 3: Library (+ cross-screen state system)==**
- Mockup: liminal-library-mockup-v5.html (canonical). Home cover module updated in liminal-home-mockup-v5.html.
- **LIB1 — Scope control is a segmented control (All · Series · Authors) in the sticky glass Library header.** Scope = entity type only. Wishlist is NOT a scope tab: scope answers "what kind of thing," ownership answers "which books" — mixing axes breaks the segmented control's semantics (what would Wishlist × Authors mean?) and creates two competing filter states. Ratified with LIB3 as the discoverability answer.
- **LIB2 — One control row: Filters · Wishlist · sort · grid/list.** Filters = chip with active-count badge, teal when live. Sort = current-sort label + single chevron opening a sheet; direction reads inside the sheet next to the active option. The double-caret + chevron combo is dead everywhere. Layout toggle = two-icon segmented pill. Header (title + scope) is the only sticky chrome; whisper count line and control row scroll away.
- **LIB3 — Wishlist quick toggle is sugar over the Ownership filter.** Bookmark chip in the control row, one tap, teal when active; bidirectionally synced with the filter sheet's Ownership section (one state, two handles). Whisper swaps to the wishlist line when active. Escalation path if still under-discovered after implementation: a Wishlist presence on Home (Home-purpose question, not Library).
- **LIB4 — Per-scope controls are removed, not hidden.** display-none semantics — no ghost layout. All: Filters · Wishlist · sort · layout. Series: sort · layout. Authors: sort only. Sort is always right-anchored, same position in every scope. Wishlist and Filters exist only in All (only titles are wishlistable; current filters are book filters).
- **LIB5 — Filters persist across scopes; state stays visible.** Book filters remain attached to All while visiting Series/Authors. A small teal dot on the "All" segment signals active filters or wishlist from any scope. Chip row (removable chips + Clear all) renders in All only. Rationale: NNG "mystery filtering" — persistent filters are fine, invisible persistent filters are not; controls that do nothing in a scope are removed per minimalism. If series-level facets ever ship, the Filters control returns to that scope with its own facet set.
- **LIB6 — Filter sheet speaks one selection language: pills.** Single-select sections: Ownership (Everything/Owned/Wishlist), Category, Status. Multi-select sections: Length, Tags, Format. Priority section (Any/High/Normal) reveals only when Ownership = Wishlist. Glass sheet per S4. The in-sheet dropdown is dead.
- **LIB7 — Tags: pills in the filter sheet are selections only.** No algorithmic "top tags." Zero selected → "Add tags" button + quiet "none selected" hint. Selected tags render as pressed pills (tap to remove) + "All tags" button carrying a count badge. Tag sheet: header shows live "· N selected," search input, counted checkbox rows, "Clear tags" action, Done returns to filters with selections intact.
- **LIB8 — Sort options are per-scope with explicit names.** All: Recently added / Title / Author / Read time / Recently published. Series: Series name / Number of titles / Recently added. Authors: Name / Most books. Trigger shows a short form (Recent, Name, Titles…); the sheet spells options out with direction hints.
- **LIB9 — List row anatomy.** 40px cover thumb (60px tall for books, 40px square for series), title = row 1 (single line, ellipsis), author = row 2, ALWAYS — no conditional stacking. Right meta column: read time + status. Rows are hairline-divided directly on the base background (S6/S7).
- **LIB10 — Status marks: icons on covers, labels in text contexts.** Grid covers: check = finished, pause = DNF, bookmark = wishlist — dark rounded-square marks, monochrome. Cover marks are NEVER text: the status display label is user-configurable via useStatusLabels and renders only where text renders (list row meta, detail page). DNF is always neutral-toned. Unread carries no mark — silence is the neutral state; DNF is history and is always surfaced (grid icon, list label).
- **LIB11 — Cover indicator zone (ALL cover modules, app-wide).** Covers reserve a top inset (34px portrait, 30px square); title is horizontally centered and starts below the zone; author centered at bottom. Indicators (status icon, count badge, High pill) live in the zone and never overlap text. Home's cover module ADOPTS this — one cover module, no variants. liminal-home-mockup-v4 superseded by v5.
- **LIB12 — Series are square.** 1:1 grid cards on the shared 3-col tracks, count badge top-right, title clamps 2 lines; 40px square list thumbs. Books = portrait, series = square: silhouette does the taxonomy. 3-col density ratified (more series per screen is the point).
- **LIB13 — Authors scope: rail, not search.** No in-scope search input — the Search tab owns author search (hard requirement carried to Slice 4). Dense 48px divided rows (name + count), sticky glass letter headers, right-edge A–Z jump rail with drag bubble; letters with no authors are dimmed.
- **STATE1 — State-block anatomy (all empty/error states, all screens).** Dead center of the content area, both axes — between visible chrome and bottom nav. Text max-width 280px. At most ONE action, ghost-pill style, 18px below the text. *(Override log: Claude proposed a fixed 72px-below-chrome offset; Marie overrode to dead-center after phone review — center is the predictable location.)*
- **STATE2 — State voice split.** Empty states speak in whisper (Georgia italic, muted): ambience, invitation. Error states lead in plain voice (system font, body color): information + direction, with whisper as the secondary line only. Errors never apologize, never blame. No exclamation marks anywhere.
- **STATE3 — Empty/Error hide dead chrome.** In first-run Empty and in Error, the count line, control row, and chip row are hidden — nothing to sort or filter. No-results keeps chrome + chips visible (the filters are the situation).
- **STATE4 — Ratified state copy (→ MICROCOPY_LIBRARY, verbatim everywhere):**
		- No results (All): "Nothing matches all of that. Loosen a filter or two." + [Clear all filters]
		- No results (Wishlist): "Nothing on the wishlist matches. Loosen a filter or two."
		- No results (Authors): "No one by that name here yet."
		- First-run (Library/All): "Your library is waiting for its first book." + [Add a book]
		- First-run (Series): "No series yet. They'll gather here when books belong together."
		- First-run (Authors): "Authors arrive with their books."
		- Wishlist empty: "Nothing wished for yet. Wish from any book's page."
		- First-run (Home): "Welcome to Liminal. Every library starts empty." + [Add your first book]
		- Error (server down): "Couldn't reach your library." / "The server may be off, or you may be away from it." + [Try again]
		- Error (offline/away): "You're away from your library." + [Try again]
	- The Add action appears only where adding is the fix (Library All, Home). Series/Authors first-run are whisper-only.
- **STATE5 — Loading = in-place skeletons.** Same tracks/rows, bg-elevated blocks, no shimmer animation (calm UX). Never a centered spinner — skeletons preserve layout, spinners relocate attention.
- **STATE6 — Failure fallbacks.** Cover/metadata partial failure degrades silently to GradientCover — never a broken-image glyph, never a toast. Server-down vs offline are distinct error copy (STATE4); implementation distinguishes fetch-fail vs navigator.onLine. Action-failure toasts deferred to Slice 8's sheets pass.
#### **==2026-07-30 — UI Redesign Mockup Sprint (Book Detail · Home · Navigation IA)==**
**Status:** Design locked via on-device mockup iteration (Book Detail v1→v5, Home v1→v4). Mockups only — no code changed, no implementation scheduled. Canonical mockups: `liminal-book-detail-mockup-v5.html`, `liminal-home-mockup-v4.html`.
**Ratified:** Marie, 2026-07-30, phone review each iteration.
##### System-wide
- **S1 · Header rule.** Section scaffolding = small-caps labels (METADATA, FILES, TAGS…); named content = Title Case (section titles on Home, note headings, series names). "ABOUT THIS BOOK" → "About" (also fixes a "books" voice violation).
- **S2 · Chip taxonomy collapse.** Only two semantic tints: content rating = warning amber (Explicit is advisory, not an error — no more red), AO3 completion = sage success. Tappable metadata (fandom, ships) = teal text links. Tags = one neutral gray family. Format badges = uniform neutral (no per-format hues).
- **S3 · Status tones.** Reading = teal tint, Finished = sage tint, Unread/DNF = neutral warm gray. DNF is NEVER failure-colored (philosophy rule; live app currently violates — see Open_Questions).
- **S4 · Glass restored and standardized.** Sticky top bars, tabs, bottom nav, and bottom sheets are frosted (16px blur over Warm A glass values). Bottom sheets = the token file's `.glass-panel` intent.
- **S5 · Whisper layer.** Serif-italic muted text (Georgia stack) is the sanctioned register for poetic microcopy: time-tier descriptors, greeting, section subtitles. One whisper per context, never stacked.
- **S6 · Divided-row pattern.** Metadata rows, file rows, reading sessions, and series entries all use the same hairline-divided rows inside one surface card — never one-card-per-row piles.
- **S7 · Cards = data groups only.** Detail-page sections are quiet surface cards; browsing surfaces (Home shelves/grids) sit directly on the base background. No mats under cover grids.
- **S8 · Light alternate = wheat.** Dark remains the canonical theme (per DESIGN_PHILOSOPHY). Light is a warm-paper alternate: base ≈ #f2ebdd family, parchment surfaces, warm-black text, teal darkened to clear WCAG 4.5:1. Covers render identically in both modes (covers are content, not chrome). Values are mockup-grade — formal token ratification pending.
- **S9 · Buttons quieted.** No full-width filled slabs for routine actions. Primary-adjacent actions render as quiet outline pills; destructive actions never exposed as bare ✕ icons in rows.
##### Book Detail (mockup v5 = canonical)
- **BD1 · Compact hero.** 118px cover left; right column top-to-bottom: series eyebrow (when in series) → title → byline → inline stat line → whisper tier-descriptor → pill row. One shared left anchor; content reachable without scrolling past a billboard.
- **BD2 · Cover lightbox.** Tap cover → fullscreen viewer (tap anywhere closes). Compact hero + lightbox is the ratified compromise for cover appreciation.
- **BD3 · Stat line order.** hours · category · rating (est. read time outranks rating for first reads). Word count removed from hero → lives in Metadata.
- **BD4 · Status control.** The 4-button segmented row is deleted. Status changes go through the hero status pill → ChangeStatusModal, restyled as a glass bottom sheet (Unread / Reading / Finished / DNF rows with status dots + check).
- **BD5 · Download.** Demoted from teal slab to a quiet outline pill beside the status pill in the hero.
- **BD6 · Sticky glass tabs.** Details / Notes / History stick beneath the top bar.
- **BD7 · Metadata table.** Label/value divided rows; Fandom + Ships as teal links; Rating + Status as the two sanctioned tint chips; everything else plain text.
- **BD8 · Collections = cover shelf.** Horizontal scroll of 92px collection covers with caption titles beneath (real collection art in-app), bleeding to the card edge. Replaces the wrapped link list.
- **BD9 · Series navigator card.** Placed after Tags, before Files; renders only when the title is in a series (paired with the hero eyebrow). Head: SERIES label + "View series →". Rows: tabular number (decimals supported) · title · one indicator — sage ✓ for finished siblings; current book = teal title + "You are here" (replaces the check; your own status already lives on the pill). Series name appears only in the eyebrow — never repeated inside the card.
- **BD10 · Files rows.** Format badge + size up front; filename demoted to muted caption; refresh/replace/delete behind one ⋯ per row. "Add files" ghost button + location caption in the card foot.
- **BD11 · History.** Sessions as divided rows in one card, newest first; finished sessions show date ranges, in-progress/DNF show start dates; partial ratings render filled stars + dimmed empties (confirm — see Open_Questions); summary stats (Times read / Average rating) as the quiet value-over-caps-label pair below the card.
##### Navigation IA
- **NAV1 · Bottom nav = Home · Library · Search · Collections.** Four tabs. Settings, Series, and Authors lose their slots.
- **NAV2 · Series & Authors are Library view scopes** (All · Series · Authors), not destinations — anchored in the Library header, not a floating sub-nav (floating pill rejected: ambiguous ownership, occludes content; kept as plan B).
- **NAV3 · Wishlist is an ownership filter** inside Library, not a place.
- **NAV4 · Browse merges into Search.** One dedicated Search tab absorbs the Browse screen; the Home/Browse/Wishlist top-tab row dies.
- **NAV5 · Add is an action, not a place.** Header + button (Home, Library); never a nav slot.
- **NAV6 · Settings = gear, top-right of Home only.** Works because top-level screens never carry a 3-dot menu (3-dots belong to detail pages) — the old gear/3-dot conflict dissolves.
- **NAV7 · "More" tab rejected.** Nothing is homeless, so nothing needs a junk drawer.
##### Home (mockup v4 = canonical)
- **H1 · One cover module.** A single 3-column grid governs the page; every cover on Home is the same size on the same tracks. No hero tiles, no mixed sizes, no inktide mosaic (retired — poster layout, not a working screen).
- **H2 · Exactly three sections:** Currently Reading + Recently Added (side-by-side card decks on tracks 1–2) and Discover Something New (standard 3×2 grid). Quick Reads and the Your Reading card are cut from Home.
- **H3 · Deck interaction contract.** Swipe left/right flips the pile (dots + progress follow); TAP always opens Book Detail — same tap contract as everywhere else. Currently Reading deck carries a 3px progress bar; both decks carry pager dots.
- **H4 · Third track = open air** (pile edges breathe into it). If it reads as a missing tile on device, the fix is a third deck, not decoration — see Open_Questions.
- **H5 · Conditional top slot — one line, never two.** Month has stats → stat doorbell line: "This month · 180.8K words · 16h 44m · 10 finished ›" (tappable → Stats page). Month has zero stats → greeting whisper only: "Good morning · 1,742 doors."
- **H6 · No captions on Home.** Covers are the interface; titles/authors live on the covers themselves.
- **H7 · Home header.** Wordmark "Liminal" (serif) left; + and gear right; glass, sticky.
##### Deferred scope (decided direction, not yet designed)
- **Stats page** — the doorbell's destination; period selector: This month / This year / Last year / All-time.
- **Library screen** — view scopes (All · Series · Authors) + ownership filter (incl. Wishlist).
- **Search screen** — absorbs Browse; design pending.
- **Light-mode token ratification** — promote the wheat mockup values into `liminal-tokens-warm.css` / `tailwind.config.js` as a formal token pass.

#### ==**2026-07-26 decisions**==
- 2026-07-26 | Dismiss-pair mechanism RATIFIED (six decisions, recon-gated): D1 pair key = unordered title-id pair (a < b), ids not normalized strings — stale pairs referencing deleted titles are inert, vanish via JOIN. D2 storage = new table dismissed_duplicate_pairs (title_id_a, title_id_b, dismissed_at, PK on pair), created in run_titles_migrations per the Phase 9A backup_history precedent (only post-launch new-table migration; idempotent CREATE IF NOT EXISTS); database.py frozen-file edit ratified here. D3 filtering = two-point, NOT uniform post-pass: inline dismissed-pair guard at the fuzzy pairwise test (titles.py:3557) + connected-components split per exact bucket. Rationale: fuzzy groups are greedy stars — only seed↔member edges were ever tested; a uniform post-pass would retain members through invented, never-tested edges. Exact buckets ARE complete graphs, so component-splitting there is truthful. D4 UI = per-group "Not duplicates" control, stored as all constituent pairs; no bulk-dismiss in v1. D5 un-dismiss = on DuplicatesPage, shows the stored PAIR list (groups have no server-side identity and don't survive the request), un-dismiss per pair — dismiss in groups, restore in pairs, asymmetry honest to storage. D6 counts and clean state are post-dismissal by construction (backend filtering) | Named risk accepted: group-dismiss on a fuzzy group stores never-compared pairs — the tap asserts "none of these duplicate each other," which is what the button says; recorded so it is not later rediscovered as a bug
- 2026-07-26 | CORRECTION: the duplicates scan threshold is 0.75 Levenshtein ratio plus substring/prefix rules and exact-author bucketing — no numeric score is emitted. The 0.80 figure belongs to the upload grouper (should_group_together, upload_service.py), a separate pipeline. Any prior note citing 0.80 for the library scan is wrong | Recon 2026-07-26; propagated from a misattributed vault line
#### ==**SortDropdown restructure decision sprint (2026-07-24)**==
- 2026-07-24 | SortDropdown ANATOMY RATIFIED: chevron direction buttons DELETED (both variants). Row semantics preserved — new field → default direction, active field → toggle. Active row renders its current-direction label (existing ascLabel/descLabel table) + 16px direction glyph, tap flips; every tap still closes. Nesting defect dies with the chevrons; direction labels become visible on mobile for the first time | The chevrons' one unique capability (one-tap inactive-field + non-default direction) cost 24–28px targets inside another button, invalid HTML, and unnamed icon buttons
- 2026-07-24 | SortDropdown EXTRACTS to ui/SortDropdown: four-consumer census (Library, AuthorDetail, CollectionDetail, WishlistTab — handoff's "one" was stale), uniform (field, dir) contract. Single option-list body, CSS variant split (hidden md:block / md:hidden, ThreeDotMenu pattern) — isMobile state + resize listener deleted. Sheet surface → bg-bg-elevated per the 2026-07-23 container ratification (resolves the component's self-disagreement). Stays IN-TREE — no portal; stacking verified clean on all four surfaces, dedup with ThreeDotMenu's portaled sheet stays parked for a session that names it. Default-direction helper EXPORTED; AuthorDetail imports it and deletes its duplicated recency list | Four consumers clears the ThreeDotMenu extraction bar with room to spare
- 2026-07-24 | All 9 provisional SortDropdown markers DELETED via extraction — ui/ is outside the strict matcher's scope; none convert, none survive. Chrome inventory 47 → 38 | Cleanest disposition available: the debt named 2026-07-23 is retired, not reclassified
- 2026-07-24 | MenuItem fit: NONE, closed. Sort rows intrinsically display selection; selected state is excluded from MenuItem's contract (locked 2026-07-22). Rows stay bespoke inside ui/SortDropdown — container-side selection styling, exactly what the exclusion prescribed | Not relitigated
- 2026-07-24 | Golden Rule 4: custom path DELETED (showCustom, onCustomSelect, both custom-row blocks, "Drag to reorder" badge — zero consumers, two of the nine markers annotated unmountable buttons), read_time SORT_OPTIONS entry deleted, default options fallback deleted — options becomes required (all four consumers pass it). Both-sides grep re-verification before each deletion | Unreachable UI carrying lint markers is the definition of GR4 scope
- 2026-07-24 | SortDropdown a11y package: trigger gains aria-expanded + aria-haspopup="listbox" + min-h-[44px] (S4b ThreeDotMenu precedent — this trigger was skipped at ~32px); rows role="option" + aria-selected inside a role="listbox" container; sheet role="dialog" + aria-modal. Color-only selection state retired as sole channel | Correct semantics, and the unnamed mobile chevrons stop being a problem by not existing
- 2026-07-24 | Microcopy: ascLabel/descLabel strings graduate tooltip-only → visible UI. Existing strings ship as-is; any NEW visible label (e.g. series order) is flagged verbatim for ratification in the session report | One ratified string = every verbatim occurrence still applies
- 2026-07-24 | RIDER (post-hoc, flagged in v0.83.0 report): sheet move to bg-bg-elevated forced inactive-row press feedback to flip to bg-bg-surface (feedback invisible otherwise). Ratified on the merits, v0.82.0 precedent | Knock-ons get recorded, not smuggled
- 2026-07-24 | Gallery demo owed: ui/SortDropdown is the seventeenth shared component with no ComponentPreview demo — breaks the all-demoed invariant. QUEUED as a rider on the next code session (not its own session) | Small enough to ride along, real enough to name
- 2026-07-25 | Merge-confirm CONTENT MODEL RATIFIED (both surfaces): confirm describes the computed outcome, not the operation. Keeping-first framing (kept title leads, teal; merging-away card muted — SOURCE/TARGET all-caps labels retired), real per-category counts, zero rows suppressed, trash line renders ONLY when files will actually move (editionFileOnDisk idiom), plus the metadata-honesty line: the kept title's details win, the other's tags and metadata aren't carried | Root cause of the mid-test scare: copy asserted a static operation ("This can't be undone") that contradicted the v0.55–v0.71 lossless semantics
- 2026-07-25 | Merge-confirm DATA SOURCING: BookDetail source-side counts from existing state (sessions/notes/bookCollections — Delete Title modal precedent, free); target side via one select-time getBook fetch (existing endpoint, no new backend surface); DuplicatesPage via find_duplicates serializer extension: has_file (stat-checked), session_count, note_count per book — STOP-gated on query cost, ships in session 2 with its consumer | No consolidated preview endpoint — four shipped endpoints already serve the data
- 2026-07-25 | CONDITIONAL CARRIES (cover, wishlist note) report POST-merge from the response body both surfaces currently discard — BookDetail Toast before navigate, DuplicatesPage numbered success state — never predicted at confirm | Predicting client-side duplicates the backend rule and drifts from it
- 2026-07-25 | Merge BUTTON PAIR unified: "Merge" (danger) / "Keep separate". DuplicatesPage's "Merge & Delete", "Cancel", and "This can't be undone." retired — the same strings v0.56.0 retired on BookDetail, finally reaching every occurrence. Voice riders: "books"→"titles", "click"→"tap", emoji strip, exclamation cleanup | One ratified string = every verbatim occurrence
- 2026-07-25 | BULK PARTIAL-FAILURE: any mid-loop merge failure auto-triggers the duplicates rescan (truth restoration — no stale rows listing deleted titles) and the error names what completed: merged M of N, then failed. No rollback pretense; completed merges were valid and stay | The page must never render titles that no longer exist
- 2026-07-25 | WISHLIST ASYMMETRY RATIFIED AS INTENTIONAL, no code: (1) wishlist-as-source stays (note-carry flow working as designed); (2) wishlist-as-target stays impossible (owned-only search — keeping a ghost over a real book would trash files to preserve nothing); (3) wishlist entries invisible to the duplicates scan is a REAL GAP, deferred — ROADMAP line: duplicates scan optionally includes wishlist entries, always as the merge-away side | Recorded so a future session doesn't "fix" (1) or (2) into a bug
- 2026-07-25 | NO shared confirm component across the two surfaces — modal vs inline row, single-pair vs aggregated-group math; consistency lives in the ratified strings | Forcing a component buys maintenance, not consistency
- 2026-07-25 | Sprint riders: api.js mergeTitles JSDoc corrected (editions haven't moved since v0.56.0), pre-token utilities (text-xs/text-sm/font-medium) modernized in touched blocks only, SortDropdown gallery demo (owed from v0.83.0) joins session 1 | Two sessions: v0.84.0 BookDetail, v0.85.0 DuplicatesPage — session-2 prompt written after session 1 lands
- 2026-07-25 | RIDER: "of reading history" trimmed from the Delete Title modal sentence as well ("…along with {N} reads and {M} notes") — same redundancy rationale as the merge-row ratification; separate string, separately ratified | Phrase-level consistency across the two destructive modals
- 2026-07-25 | v0.85.0 microcopy RATIFIED: sums variants ("{X} reads and {Y} notes move over." family, verb by total), trash clause ("{F} file(s) go(es) to the trash folder."), "Merged {M} of {N}, then failed" (0-of-N edge accepted as honest), rewritten help strip, "Your library is clean.", "View title details", "{n} titles". Made calls ratified: triage-row zeros render deliberately, strict has_file === false check, failure detail console-only. TWO OVERRIDES: "Please select a title to keep" → "Select a title to keep" (no "please" in UI — same register rule as the exclamation ban); "Merge into Selected" → "Merge into selected", help-strip quote follows (one ratified string = every verbatim occurrence) | Voice doc enforced over minimal-change inertia
- 2026-07-25 | v0.85.0 rider verdicts: MarkFinishedModal VERIFIED using ui/StarRating (import :5) — Open_Questions item closes with evidence; BookDetail Toast live-region fix confirmed STRUCTURAL (single-return collapse required) — stays in Open_Questions for a session naming BookDetail; DESIGN_SYSTEM records no gallery coverage — ROADMAP is the ledger of record for demo counts | Report-only riders resolved as designed
- 2026-07-26 | Conversion auto-scan WAITS for 10.1b — not shipped standalone. The handoff-era "dispatcher rewrites the call site" rationale is retired as wrong (dispatch lives inside extract_metadata(); call sites survive). Actual grounds: EPUB-only auto-scan silently no-ops on the library's non-EPUB majority; the extraction output contract (value/source/confidence, dispatcher shape) is an open 10.1b agenda item that could churn a fresh call site; the 2026-07-20 vault note already absorbs conversion-flow extraction as sprint input; date_added semantic opens the same conversion path — one sprint, one edit. Backfill inside 10.1b retroactively fills anything landing empty meanwhile, so nothing is lost, only deferred | Standalone steelman recorded and rejected: interim manual fill is recovered by fill-empty-only backfill

#### **==S4b Ratifications + Sprint Close (2026-07-24)==**
- 2026-07-24 | S4b shipped v0.82.0: raw-< button> STRICT at zero — 47 annotated markers (9 provisional-SortDropdown, 2 ratified-bespoke, 33 plain chrome, 3 honesty), 20 structural exemptions (6 role="option" + 14 element-children), 0 unmarked, 0 ignore lines, all ten categories pass | The category reaches 0/pass like alert() did — the sprint's stated goal, verbatim
- 2026-07-24 | Priority ✓ RATIFIED retroactively as the save-confirmation it always was: priorityStatus 'saved'→✓/'error'→! predates the sprint, the error twin is the only inline failure surface for priority saves. Kept on no-silent-failures merits, placed as a flex sibling of the chip. My residue hypothesis was wrong; the STOP condition is why it didn't ship wrong | Recon beats pixel diagnosis, again
- 2026-07-24 | Popup wrap fix RATIFIED: w-max on the absolutely-positioned popup (shrink-to-fit was clamping to the trigger's width). Container-level, MenuItem untouched, contract intact | The fix lived exactly where the ruling said fixes live
- 2026-07-24 | Matcher tightenings RATIFIED (both via tighten-and-report): svg/span//Icon$/-named tags never qualify as content children, nested buttons in either direction never exempt. First draft would have hidden half the SortDropdown defect and leaked its trigger + the drag handle | The STOP existed for exactly this
- 2026-07-24 | Sixth role="option" RATIFIED: WishlistForm's element-childed title row is a genuine combobox option alongside the ratified five. Container role="listbox" additions NOTED as the unratified remaining half — queued for the a11y pass, not snuck in | Half-done a11y is fine when the half is honest
- 2026-07-24 | Badge tint table gains "danger" (match-type pill), "Close" microcopy ratified (BookLinkPopup ✕), sheet Cancel text-base→text-body, trigger p-3/44px | The anticipated one-liners, anticipated correctly
- 2026-07-24 | SettingsRow bg-white toggle knob → post-sprint queue (default-palette literal now living in ui/, invisible to A1; byte-identical move was correct, fix is its own line item) | Moves don't edit; the advisory ledger works
- 2026-07-24 | SPRINT CLOSED. Scorecard: 124 pre-C1 → 120 → 103 (S2) → 99 (S3) → 95 → 70 (S4) → 47 marked / 0 strict. Shipped: MenuItem, ThreeDotMenu (extracted, 3 consumers, 10.0.14 true), Badge (rebuilt, 8 sites), SettingsRow rehomed, SegmentedControl ×3 + StarRating adopted, gallery 15 sections, ghost borderless (61 sites). STOPs honored: 5 across the sprint, 0 improvised fixes. v0.75.0–v0.82.0, eight versions | The design system stopped existing on paper

#### **==S4 Ratifications (2026-07-23)==**
- 2026-07-23 | S4 shipped v0.81.0: MenuItem + ThreeDotMenu extracted (10.0.14 finally true) + Badge rebuilt (solid/tint/outline/muted, exactly at the 4-variant cap) + adoptions. Raw-button 95 → 70, all units attributed, net −189 lines while shipping two new components | The dedup story in one number
- 2026-07-23 | ThreeDotMenu items `icon` field RATIFIED retroactively — flagged, not silent: required the moment CollectionDetail adopted (its five rows carry icons; MenuItem's icon slot was elected for those menus). Two locked contracts composing, not a capability change | The alternative was silently deleting shipped icons
- 2026-07-23 | BookDetail :1932/:2880 segment sites RECLASSIFIED: not SegmentedControl adoption targets — they are the ratified v0.60.0 status toggle (per-status active tokens, 2×2 long-label flip, disabled race guard) and the session form's semantic actives. The drift-check census mislabeled ratified bespoke controls as hand-rolled duplicates. DISPOSITION: B4 chrome markers in S4b, annotated as ratified-bespoke | The STOP prevented reverting two ratified decisions to satisfy a census label
- 2026-07-23 | Priority-popup selected-tint death ACCEPTED: MenuItem excludes selected state by decision; the trigger chip displays the current value | The exclusion list working as written
- 2026-07-23 | DuplicatesPage :220 match-type pill ADOPTS Badge in S4b — same shape as Same Author, missed the 7-site census by a line | Eighth site, one-line conversion
- 2026-07-23 | Reviewer findings DISPOSED to S4b as ratified chrome tidy-ups: shared trigger tap target ~32px → 44px (now on three surfaces, worth fixing once in ui/), sheet Cancel raw text-base → token size | Pre-existing, carried verbatim, now cheap to fix at the shared source
- 2026-07-23 | Microcopy RATIFIED verbatim: "More actions", "Filter by priority", "Books per row (mobile)", "Card style" | All honest labels

#### **==Drift-Check Ratifications + S4 Rulings (2026-07-23)==**
- 2026-07-23 | Drift check CLEAN: count chain reconciles 124→120→103→99→95 with every link attributed, zero ignore lines, frozen set untouched since v0.74.0, all S4 targets content-drift-free (S2/S3's BookDetail work landed :1887–:2555, below ThreeDotMenu territory). Recount is the truth going forward: B3a 21 · B3b 13 · B4 61 = 95 | The gate did its job: two real surprises caught before prompts were written
- 2026-07-23 | Priority-popup pair (BookDetail :1839/:1850) RECLASSIFIED B4→B3b and converts with MenuItem — they are menu items, the popup is just a sixth container; MenuItem is container-agnostic by design. B3b denominator: 15 | Also explains the triage's ±2 mirror: its 15 was 13+2 all along
- 2026-07-23 | Text-only option rows (5: WishlistForm ×2, AuthorChips, TagsMultiSelect, UnifiedEditModal ×2 minus overlap — per drift-check list) → role="option" ADDED (a11y correction; comboboxes should mark options) and the B3a matcher gains a second criterion: element-children OR role="option". Zero markers | Two written criteria beat five markers pretending to be exceptions
- 2026-07-23 | SettingsRow MOVES to ui/ (S4b): already a shared primitive, wrong folder not wrong shape; internals exempt under the existing rule, same logic as the extraction | git mv + import updates, not a rebuild
- 2026-07-23 | BookLinkPopup :92 close × → IconButton with aria-label (S4b): IconButton-shaped, missing label is a real a11y gap, one site. ManualEntryForm :255 banner-dismiss precedent doesn't bind — different anatomy (banner vs modal header) | Converting costs less than debating its category
- 2026-07-23 | SortDropdown PARKED behind the flip: nested buttons (options :145/:222 wrap chevron buttons :155/:165/:232/:241) are invalid HTML and a genuine pre-existing defect, but restructure is its own scoped session. Its 9 sites take chrome markers annotated "provisional pending SortDropdown restructure"; restructure joins the post-sprint queue AHEAD of merge-confirm | A pre-existing defect doesn't hold the flip hostage
- 2026-07-23 | WishlistTab priority segments ×3: SegmentedControl adoption candidate, recon-gated in S4 — adopts if props fit without contortion, takes markers if not | Same pattern as the S1 four; cheap either way
- 2026-07-23 | S4 SPLITS at the adoption boundary, ratified: S4 = MenuItem+election, B3b 15, ThreeDotMenu extraction (kills 4 BookDetail sites; dots adoption kills 2 more), Badge rebuild+7-site adoption, SegmentedControl 4(+3), star picker→StarRating. S4b = matcher rescope, role additions, SettingsRow move, BookLinkPopup IconButton, chrome markers, STRICT FLIP, sprint scorecard. S4b's prompt is written only after S4's report — its marker arithmetic depends on S4's outcomes | Writing both now would bake stale arithmetic into the flip
- 2026-07-23 | MenuItem VISUAL ELECTED: w-full flex items-center gap-3 px-4 py-3, optional leading icon (slot collapses when absent), text-text-primary rest / text-action-danger danger / disabled dims, hover bg-bg-surface, no rounding (container clips), type="button" always. Containers standardize on bg-bg-elevated during extraction, resolving the five-site hover split with one value. BookDetail's text-text-body outlier and raw text sizes die in conversion; py-3 at base size clears 44px | CollectionDetail's shape won the election with the sheet's mobile sizing folded in

#### ==**2026-07-23 — S3 Ratification Round (v0.79.0)**==
- **Census proceed call — RATIFIED.** Triage's 15 resolved to 4 live convertible B2 sites. Five of nine subtractions rested on ratified decisions / component contracts rather than shipped-change-shaped deltas; ratified as valid "named cause → proceed" under the fresh-census-decides rule. No reading of the census permitted converting more than 4. Precedent: attribution clause satisfied by any named, verifiable cause — shipped change, ratified decision, or structural contract exclusion.
- **New microcopy — RATIFIED verbatim:** aria-labels "Edit collections", "Edit notes", "Edit reason" (BookDetail section pencils + TBR pencil). One-ratified-string rule applies to all future verbatim occurrences.
- **Citation correction (records only).** The section-pencil convention sits under the 2026-04-12 Fix-Session-3 header, not 2026-04-13 as cited in the 2026-07-22 Decision Sprint entry, the S3 prompt, and the v0.79.0 CHANGELOG entry. Substance identical; correct citation going forward is 2026-04-12. Prior entries stand uncorrected per append-only discipline.
- **SearchBar.jsx deletion — APPROVED** as standalone Golden-Rule-4 session, after S3 deploy/phone-test/push. Deletion prompt must verify whether SearchBar.jsx exists in the live Docker volume; if present, manifest carries a Volume DELETION line. Raw-button count 99 → 95 on landing. Drift check runs after deletion, against the post-deletion state.

#### **==S2b Ratifications (2026-07-22)==**
- 2026-07-22 | S2b shipped v0.78.0: ghost borderless via one class-string edit in ui/Button — border border-border-default deleted, rest/hover/active/sizes untouched, zero call-site edits | The variant fix as ratified, one line
- 2026-07-22 | Blast radius CORRECTED and proceed RATIFIED retroactively: ghost has 61 usages across 32 files, not S2's ten — dominated by the modal-footer Cancel population, FilterDrawer Clear-all, FileDropZone picker. Claude Code proceeded on the ratified mechanism (fix the entry point) with a self-flag; correct handling. All 61 inherit quiet; footer Cancels now read as text buttons beside filled primaries — the classic modal register, pending phone verdict | The decision named ten sites but ratified a mechanism; the mechanism was always going to find every consumer
- 2026-07-22 | Compensating-class sweep across all 61 sites: clean — every ghost className is layout-only. Border load-bearing nowhere (heights pinned by min-h-*); StatusLabelsModal's right-flush shifts ~1px, sub-perceptual | The zero-call-site-edits promise held at 6× the assumed population

#### **==S2 Phone-Test Findings (2026-07-22)==**
- 2026-07-22 | GHOST VARIANT RESTYLED to borderless (S2b rider): text at rest, hover bg-bg-elevated, no border. S2's phone test surfaced that ghost renders bordered — ten quiet text actions became boxes, fighting the calm-UX philosophy the variant mapping was built on. Fix lands in ui/Button so all ten sites and every future ghost inherit; zero call-site edits | The variant betrayed the mapping; fix the shared entry point, not the ten consumers
- 2026-07-22 | TBR "Why this one?" Edit RECLASSIFIED to S3: section edits are pencil IconButtons at #5c5752 (ratified 2026-04-13, reaffirmed 2026-07-16) — a text Button of any variant is nonconforming there, and was before S2 touched it. "+ Add a reason why" stays a ghost text button (add-affordance, no section anchor) | S2 faithfully converted a convention violation; S3 fixes the convention
- 2026-07-22 | Phone test c7/c9 gaps recorded as test-plan misses, not defects: CollectionModal's Info/Hide renders only for automatic collections; SearchModal's filter-escalation row lives in the search overlay. Both inherit the ghost restyle | The plan pointed at the wrong doors

#### **==S2 Ratifications (2026-07-22)==**
- 2026-07-22 | S2 shipped v0.77.0: all 17 B1 sites → ui/Button, raw-button 120 → 103, zero color overrides needed, nothing parked as ambiguous. Ten previously link-styled actions became honest ghost buttons — the Button-vs-Link violation the category existed to catch | The conversion the whole lint category was for
- 2026-07-22 | B1 CENSUS CORRECTED 19 → 17, attribution ratified: the triage's category sums total 124 (the pre-C1 baseline); C1's AcquireCard dormant-block deletion (v0.73.0) took two text-labeled action buttons ("Mark Finished", "Status ▾") with it. STANDING COROLLARY: every remaining triage category inherits the same recount caveat — B2 is likely 13, not 15 (the dormant block also held two icon-only buttons). S3 and S4 prompts state their counts as pre-C1 numbers with fresh-census-decides | The attribution clause used exactly as designed; count drift with a named cause proceeds, unexplained drift stops
- 2026-07-22 | AddPage Dismiss register change RATIFIED: red-tinted banner dismiss → neutral ghost. The danger tint belonged to the banner, not the act — dismissing a benign notice is not destructive. Flagged because it changes shipped UI register, not just markup | Variant mapping is allowed to correct a register error it finds, but the correction gets its own line
- 2026-07-22 | CollectionPicker's dashed "New Collection" parked as NOT-B1 on structural grounds (element children + distinct create-slot affordance; conversion would need overrides — the misclassification tell). Lands wherever its true category takes it, likely B3a rescope | The rules classified it; nobody had to argue
- 2026-07-22 | MarkFinishedModal Clear grew ~20px → 36px sm per the tap-target rule — phone-check flagged since that modal is also S4's StarRating adoption target and gets touched twice | Two edits to one small modal deserve two looks

#### **==S1 Ratifications (2026-07-22)==**
- 2026-07-22 | S1 shipped v0.75.0 as a PARTIAL: demos + recon + doc riders landed; gallery exit STOPPED on a verified z-order collision (real UnifiedNavBar sticky z-20 vs de-stickied demo frames keeping z-20 — later DOM sibling wins the paint). Stop was correct; no fix improvised | The STOP condition existed for exactly this
- 2026-07-22 | Badge SURVIVES to S4 rebuild — recon count 7 non-frozen adoptable display-pill sites (WishlistTab PriorityBadge, UploadSuccess result pills, ChangeCoverModal source pill, BookDetail format badges + TBR category chip, CollectionDetail type badges, DuplicatesPage Same Author), miles past the ≥2 gate. Interactive filter pills and numeric/icon overlays excluded as different component classes — that boundary is now census precedent for the S4 adoption scope | The orphan was out-competed by copy-paste seven times over; that is the textbook shared-component case
- 2026-07-22 | Census correction RECORDED: ImportPage was deleted v0.62.0 — the decision sprint's known-site list named it stale. DESIGN_SYSTEM §3 Badge "Common mistakes" still cites it; fix rides with S4's Badge §3 rewrite, not before | Known-lists drift; greps don't
- 2026-07-22 | Demo-frame fix RATIFIED: extend both ComponentPreview demo-frame wrappers to [&>div]:!relative [&>div]:!top-auto [&>div]:!z-0 — demos lose only the stacking win. The alternative (touching UnifiedNavBar's z or wrapping it) rejected: the bar's look is deliberately non-overridable and a wrapper breaks stickiness | One line in the demo chrome beats surgery on a shared primitive
- 2026-07-22 | Settings copy RATIFIED: new bottom grouping "Developer" after Trash, row label "Component gallery" | First developer-facing surface in Settings; the grouping name should age fine
- 2026-07-22 | S1 made-calls RATIFIED wholesale: StarRating section pre-existed (prompt premise stale — only FormField's placeholder box was actually empty, filled with a real interactive md), README rider pre-satisfied by caa2d2b (version restamped only), ARCHITECTURE route-list fixes + gallery-claim rescope, FRONTEND_AUDIT_S12 reported-not-edited, 0.74.0 CHANGELOG header consumed-and-restored (numstat-verified byte-identical) | Nothing contested
- 2026-07-22 | MenuItem dump COMPLETE — spread recorded for the S4 spec: three row paddings (py-2/py-3/py-3.5), icons in 2 of 5, hover fill split by menu bg, danger in two shapes, type="button" inconsistent, BookDetail alone on raw text sizes + text-text-body. The locked contract's `to` prop has no precedent in any site — all navigation hand-rolls callbacks, as the ruling predicted | Five implementations, zero agreements; the comparison will be an election, not a merge

#### **==Adoption Sprint Decision Sprint (2026-07-22)==**
- 2026-07-22 | Badge verdict is RECON-GATED, not decided: S1 counts non-frozen adoptable badge-like render sites (WishlistTab, ImportPage, UploadSuccess local implementations + any others the grep finds; frozen-BookCard-family sites excluded from the count). ≥2 → REBUILD on tokens in S4 (className-composable, zero inline styles, mode API may survive but no code reuse assumed); <2 → DELETE in S1 under Golden Rule 4, gallery demo dies with it | Killing it now would be a ComponentPreview-shaped decision — a delete premised on vibes in a sprint whose whole method is recon-before-verdict; the recon is one grep in a session that is mostly greps
- 2026-07-22 | Badge scope note for the record: Badge is a < span> and contributes zero to the raw-< button> count. It is in this sprint as an orphaned ui/ component, not as button work — the strict-flip scorecard does not include it | Keeps the sprint's success metric honest
- 2026-07-22 | MenuItem CONTRACT LOCKED, visual deferred to S4: renders < button type="button">, lives in ui/. Props: children, icon (leading slot), onClick, to (renders Link with identical styling — SettingsRow precedent), danger (aligns with ThreeDotMenu's ratified danger flag), disabled, className (LAYOUT ONLY, Button's contract verbatim). Container-agnostic: owns item concerns only (padding, 44px hit target, danger color, disabled dimming); sheet chrome, dividers, section headers belong to the container. DELIBERATELY EXCLUDED, written as excluded in DESIGN_SYSTEM "When NOT to use": trailing slot, submenus, selected state, roving focus — additive later is cheap, removal means auditing every consumer. The moment it grows a variant="sheet" prop it has failed | to is the future-proofing that matters: menu items that navigate already exist, and without it people hand-roll onClick navigate, breaking link semantics
- 2026-07-22 | MenuItem visual spec is S4's, built from S1 RECON: the S1 prompt dumps verbatim item markup from all five implementations (BookDetail ThreeDotMenu, CollectionCard, CollectionDetail, CollectionsTab, BookContextMenu); mockup between S1 and S4; spec finalizes at S4 open against current code | Five implementations already exist — this is a comparison, not an invention, and it needs the real markup
- 2026-07-22 | Raw-< button> exemption architecture is THREE MECHANISMS, not a blanket: (1) B3a content rows (19) → MATCHER RESCOPE, zero markers — a < button> whose direct children are elements rather than a text label is a tappable content surface Button/IconButton would fight; (2) B4 chrome (56) → SHRINK STRUCTURALLY first by extracting ThreeDotMenu to ui/ThreeDotMenu.jsx in S4 (chrome lands in ui/ and is exempt under the existing rule; ROADMAP 10.0.14's checked-but-false extraction becomes true), THEN written per-site chrome markers on the remainder, same pattern as A6 active-ignores; (3) everything else must be Button / IconButton / MenuItem. STRICT = zero unmarked, unrescoped raw < button> outside ui/ and frozen files | A single blanket exemption for 75 of 120 sites fails the same test that killed the unbracketed-hex rule's first draft; post-flip the category's job is making the NEXT raw button impossible to ship silently
- 2026-07-22 | B4 survivor count after extraction is an S4 RECON NUMBER with a stop condition, not a promise — nobody knows today how many of the 56 outlive the ThreeDotMenu move | The marker count is evidence-priced, like everything else in this sprint
- 2026-07-22 | Sequencing RATIFIED: S1 gallery+recon → S2 B1 19→Button → S3 B2 15→IconButton (clears the 4 A6 stroke ignores) → S4 MenuItem+B3b 15+adoption+extraction+strict flip. AMENDMENT: Badge rebuild moves S1→S4 alongside its adoption (only the delete branch executes in S1). MANDATORY drift check between S3 and S4: both S2 and S3 hammer BookDetail (21 sites) and S4's flip is arithmetic on counts they change | Shipping a rebuilt component in S1 that nothing imports until S4 just re-creates the orphan with better manners; the drift check also lands exactly on the ~3-session prompt-batch boundary
- 2026-07-22 | S1 SCOPE approved with amendments — gallery exit is a real UnifiedNavBar on /dev/components with back affordance to Settings (route stays OUTSIDE ConnectedApp; demo navbar stays inside its de-stickied demo frame). Settings link ships same session AFTER the exit phone-verifies — R5 hard gate holds. Full S1: exit+header, Settings link, 4 missing demos (AuthorInput, ChipInput, FileDropZone, SegmentedControl), StarRating placeholder slot, Badge recon+verdict, MenuItem five-site recon dump, README refresh rider, DESIGN_SYSTEM error-register split (ratified 2026-07-19, still unlanded), doc-accuracy check on ARCHITECTURE.md:223 + FRONTEND_AUDIT_S12.md:189/:55 now that the gallery becomes reachable | Smaller change than moving the route inside the shell, and "every routed page gets UnifiedNavBar" was already the rule

#### **==Non-EPUB Extraction: Scoping (2026-07-20)==**
- 2026-07-20 | Non-EPUB metadata extraction + category detection gets its own phase, NOT folded into Phase 10 | Everything except external ISBN lookup parses bytes already on the NAS — zero network. Bundling offline work into an integrations phase inherits 10.x scope sprawl and queues behind four other integrations
- 2026-07-20 | Framing is "completion of S15," not a new feature | S15 made mobi/azw3/html visible as editions; this makes them legible. Same arc, next step
- 2026-07-20 | External ISBN lookup (Open Library / Google Books) is the ONE seam that stays with 10.3 External Book Search | Sequence, don't merge — the extraction phase hands off at that tier
- 2026-07-20 | Parsers live in a new module; metadata.py gets ONE ratified ~10-line dispatch edit at the top of extract_metadata() | Immature code inside a frozen file either blocks tuning or erodes the freeze. Dispatch goes inside the frozen entry point (not at call sites) per the S15 shared-write-path lesson — sync/upload/rescan all inherit, zero missed sites
- 2026-07-20 | Freeze follows maturity, not filenames — new module either graduates into metadata.py or inherits the freeze once stable | Prevents a permanent second-class extraction tier
- 2026-07-20 | Known-author category prior survives but is demoted: never auto-sets, requires unanimous history + n>=3, abstains on split history | The senlinyu problem (Alchemised=Fiction, Manacled=FanFiction under one name) is over-represented here because P2P authors are common in this fandom. File-level markers outrank the prior, so most collisions self-resolve
- 2026-07-20 | Prompts NOT written now — scope only, sprint runs after batch-3 closes | Locking a decision is cheap; prompts go stale past ~3 sessions and batch-3 has B5 + C ahead

#### **==C2 Ratifications + Batch-3 Close (2026-07-20)==**
- 2026-07-20 | C2 shipped v0.74.0 and CLOSED BATCH 3 (v0.62.0–v0.74.0, 13 versions / 5 days, 5 unplanned insertions all containment-or-honesty). All 7 made-calls ratified: A6 designation + A1–A6 label, sanction marker as start/end pair, E5 at both sites and E6 at all 7 (one ratified string = every verbatim occurrence — now the standing read), MICROCOPY organization (suites in place, Diagnostic register subsection, new Wishlist→Library section, toast double-listing), ROADMAP blockquote label kept, Current State rows = containment + conversion | Clean closer; both C1 stops resolved inside the batch as promised
- 2026-07-20 | C2 manifest defect: CHANGELOG/ROADMAP misfiled as repo-only — the copy convention lived only in vault Decisions, invisible to Claude Code. Deploy-manifest bucket rule + git add . rule added to CLAUDE.md; vault is for Marie and Claude, CLAUDE.md is for the agent — rules the agent must obey go there or they don't exist | A rule the executor can't see isn't a rule
- 2026-07-20 | Mixed-register join advisory recorded, not acted on: upload.py's semicolon-joined per-book messages can mix sentence-cased E6 strings with the lowercase v0.69.0 wishlist string. Both individually approved; harmonization belongs to the next microcopy window with the pre-v0.63.0 backlog | One string outside the ratified list stays outside the ratified list

#### **==C1 Ratifications + Item-8 Rescope (2026-07-20)==**
- 2026-07-20 | C1 shipped v0.73.0: TruncatedText deleted (the text-teal-400 lint item and the cool-fade bug died with it), AcquireCard dormant block deleted, IconButton muted variant replaces both ! overrides, danger border on the failed label field, lint strips comments (true baseline 124, now 120), bare confirm( matched. All three made-calls ratified: EditIcon/handleEdit/showEdit deletion (the reachable-only clause was the spec, not the example list), category label rename, Fourteen→Thirteen | Two stops, both correct
- 2026-07-20 | Settings gallery link DEFERRED to adoption sprint session 1 with a hard gate: the link never ships without a working mobile exit on /dev/components (R5 proved there is none — the route sits outside ConnectedApp, no BottomNav, and the gallery's demo navbar is not navigation). The sprint opens the gallery anyway for the four missing demos; header + link ride it | Linking to a room with no door is worse than not linking
- 2026-07-20 | Unbracketed-hex rule RESCOPED instead of ignore-carpeted: the matcher targets hex in className strings and SVG stroke=/fill= attributes only. Inline-style values and generation-data constants (CollectionGradient lanes, SeriesCard fallbacks — frozen-BookCard family) are code-reviewer judgment, not lint; index.css's token-mirror block gets a sanctioned marker. Under this scope the real findings are BookDetail's four strokes → written active-ignores naming the adoption sprint. Ships as C2's one code rider | 28 of 32 recon sites were data the rule mis-framed; a lint that needs 28 exceptions is measuring the wrong thing
- 2026-07-20 | Badge is delete-or-REBUILD, not delete-or-adopt: zero production importers AND 8 inline-style hexes — adopting it as-is would spread token violations. Verdict belongs to the adoption sprint | The orphan is also the outlaw

#### **==C Pre-Decision Recon Ratifications (2026-07-20)==**
- 2026-07-20 | ComponentPreview KEPT and linked from Settings — my delete recommendation is withdrawn: it was premised on the gallery being stale, and recon proved the opposite (zero prop mismatches, zero lint findings in any category, every button already a shared component). It is unused because it is invisible, not because it is useless | Wrong premise, wrong call; the recon existed to catch exactly this
- 2026-07-20 | STANDING RULE: a ComponentPreview demo does NOT count as a consumer for dead-code purposes (Golden Rule 4). Gallery-only usage is zero usage; the gallery is edited to follow deletions, never used to block them | This is the whole reason CollapsibleSection survived v0.58.0's halt-condition for fourteen versions
- 2026-07-20 | ui/TruncatedText DELETED (gallery demo removed with it). v0.58.0 killed truncation on BookDetail from daily use — scrolling is free, hiding content is not — so the component embodies a rejected pattern with zero production consumers. If truncation ever returns it is a new component built for the real case. C's text-teal-400 item dies with the file | Same ruling as AcquireCard's dormant block: reserved-for-someday is not a consumer
- 2026-07-20 | The raw-< button> count is a SHARED-COMPONENT ADOPTION problem, not a button problem — Badge 0 importers, SegmentedControl hand-rolled 4× as raw buttons, StarRating hand-rolled in MarkFinishedModal while ui/StarRating exists. Named post-batch sprint (~3 sessions): convert B1 19 + B2 15, build MenuItem and convert B3b 15, adopt the orphaned ui/ components, then EXEMPT B3a + chrome by rule so the category can go strict and reach 0/pass like alert() did | 125 was never 125 violations; the number carried no signal because nobody had classified it
- 2026-07-20 | Ratchet confirmed as the interim rule until that sprint: raw-< button> stays report-only, delta-0 becomes an enforced verification line, the count may fall and never rise | A holding pattern with a destination, not a shrug
- 2026-07-20 | True raw-< button> count is 124 — the lint matcher counts the string inside SettingsRow.jsx's doc comment. Matcher fix folded into C1; the baseline restates as 124 | The number has been wrong since S13 and nobody noticed, which is the report-only problem in one sentence

#### **==B5 Ratifications (2026-07-19)==**
- 2026-07-19 | v0.72.0 confirmed as B5's number — v0.70.0/v0.71.0 were consumed by the D-fix and aftermath sessions, and ROADMAP had already recorded the re-target. Fourth slide | Claude Code followed the shipped record over a stale prompt; that's the rule working
- 2026-07-19 | ERROR REGISTER SPLIT ratified as a design-system rule, not a session one-off: LOAD failures (passive — the user didn't act) render quiet text-text-secondary + Try again inside the failed region; ACTION failures (the user did a thing and it didn't take) render the danger banner at the point of action. The v0.63.0 banner precedent governs the second register only | A failed page-2 fetch is not the same event as a failed save, and shouldn't wear the same chrome
- 2026-07-19 | REJECTED as shipped: the failed status-label field keeps its default border. Per the register split it is an action failure, and DESIGN_SYSTEM Forms specifies the danger border on error — the field-level role="alert" is necessary, not sufficient. One-line fix folded into C, not an amend | Half a register is a worse signal than none
- 2026-07-19 | AcquireCard's dormant block (fileUrl/hasFile, per-status showDownload, DownloadIcon, handleDownload, onEditSession/onMarkFinished/onChangeStatus and their buttons) is DEAD CODE under Golden Rule 4, not a reserve — it was held for S16, and S16 closed at v0.60.0 without claiming it. Deletion folded into C alongside the emoji Acquire-button copy fix, same file | Four of the file's five raw-button rows are in that block; the microcopy pass has to open the file anyway
- 2026-07-19 | Item 4 closed as Branch B: SortDropdown's in-tree sheet is structurally sound on all four rendering pages (no ancestor creates a stacking context; the v0.65.0 trap required one). Recorded in CHANGELOG + ROADMAP so the duplicate-implementation note stops resurfacing as a stacking suspicion. Dedup vs ThreeDotMenu's portaled sheet stays post-batch | Verified-no-change is a result, and an unrecorded one gets re-investigated
- 2026-07-19 | Library:181 getCategories parked correctly per the audit stop rule (an honest surface needs FilterDrawer, outside the named set) — moved to Open Questions so it outlives the session report | A park that exists only in a report is a deletion with extra steps
- 2026-07-19 | DEPLOY MANIFEST RULE: manifests must split VOLUME-COPY files (frontend/src, backend/) from REPO-ONLY files (CHANGELOG, ROADMAP, docs/), and name volume DELETIONS separately from repo deletions. lcheck diffs only the first set, so an undifferentiated list leaves Marie guessing at exactly the step lcheck cannot verify | The manifest is a copy instruction, not an inventory
- 2026-07-19 | AMENDS the deploy-manifest rule above: CHANGELOG.md and ROADMAP.md go to the volume too, per standing practice since day 1. Repo-only means docs/ (incl. DESIGN_LINT_REPORT.md). The split that matters is still lcheck's: it diffs frontend/src and backend/ only, so anything outside those two trees is copied on trust | Wrong bucket, right rule

#### **==D-Fix Ratifications (2026-07-19)==**
- 2026-07-19 | D1(b): merge converts the source's tbr_reason into a notes-table note on the kept title (prefix "From the wishlist: " — microcopy-flagged) instead of carrying the column. Lost note recovered manually from the pre-merge backup | tbr_reason was the covers bug one column over; conversion beats carry because "Why this one?" is wishlist-register copy that would render on an owned book, and a real note survives every future flow that already handles notes
- 2026-07-19 | D2(a): wishlist familiar matches start UNRESOLVED — chooser + attention banner + the existing Add gate, which already blocks null-action cards and was simply never armed (AddPage:274 auto-resolved familiar matches to 'new'). Owned-match default unchanged | (b) rejected: a fuzzy false positive would silently convert the WRONG wishlist entry — a data-corrupting default; (c) rejected: changes long-standing owned behavior for no defect
- 2026-07-19 | D3(i-R): both upload routes refuse same-format duplicate files per-file with honest messages (LinkToTitleResponse gains a message field; AddPage renders the real response — the synthetic result dies) — the orphan _1 class is closed at the source. NEW per-format [Replace file] action in the Files section: trash-first swap, edition re-pointed + re-stat'd, metadata NEVER touched by a file swap, inline confirm mirroring Remove Format | Marie's fanfic-update workflow needs deliberate replace, not upload-flow overwrite — auto-overwrite stays dead per v0.68.0; the two-step (Remove → Add) becomes one tap
- 2026-07-19 | Fix session ships as v0.70.0; B5 renames slide to v0.71.0 | Data-loss and honesty fixes outrank renames
- 2026-07-19 | v0.70.0 riders, all ratified: Replace file uses INLINE confirm — Remove Format's modal is the recorded outlier, not the pattern (spec misdescribed the precedent; the session followed the S10 rule over the wrong description, reviewer concurred) · [Choose new file] styled secondary, not danger — trash-first recoverable swap, calm UX wins · route is POST /editions/{edition_id}/replace-file per the editions family · Replace renders only on file-backed editions; legacy 'ebook' rows get a migration-first refusal, and the duplicate pre-check deliberately ignores epub-vs-legacy-'ebook' (S15.2b defer path preserved) · merge response reports wishlist_note_converted · ROADMAP footer drift (stuck at v0.58.0 since Jul 14) aligned | Prefix shipped per rider: "Why this one (from the wishlist): "
- 2026-07-19 | Replace-file failure string fired on a same-name swap that left a working edition — v0.71.0 determines cosmetic misreport vs real post-trash failure masked by the identical filename; Replace quarantined and trash emptying held until diagnosed | Same-name success is indistinguishable from same-name partial failure from the UI
- 2026-07-19 | Done-page header reflects the OUTCOME: all-refused/all-error renders a neutral "Nothing was added" with no green check, partial success renders honest counts, full success unchanged | Green check + "0 stories added" + an ERROR row is a status lie wearing a party hat
- 2026-07-19 | Wishlist-match primary action relabeled "Move to library" (owned matches keep "Add to Existing") — frozen BookCard copy edit, ratified | The card's own verb, two words, names the outcome
- 2026-07-19 | Note prefix v2: "Why this was on the wishlist: " supersedes the v0.70.0 string | Past tense — the book isn't on the wishlist anymore
- 2026-07-19 | v0.71.0 root cause on record: the v0.70.0 replace row-update wrote editions.file_size — a column that does not exist (edition sizes are stat'd at read time; that column belongs to backup_history). Every swap failed AFTER trash + land; same-name swaps masked it because the never-updated row already pointed at the right path. Fix: path-only UPDATE. The quarantine call was correct | The failure string was telling the truth the whole time
- 2026-07-19 | HARNESS PRINCIPLE (locked; also in Claude Code memory): test schemas are built from the real init_db, never hand-rolled — the v0.70.0 harness encoded the same wrong column assumption as the code and proved a bug-shaped success | A hand-rolled fixture can only confirm what you already believe
- 2026-07-19 | Aftermath consumed v0.71.0; B5 renames slide again, to v0.72.0 (prompt reissued) | Third slide — the queue adapts, the record doesn't

#### **==B2 + Containment + Wishlist Conversion (2026-07-17/18)==**
- 2026-07-17 | v0.67.0 ratifications: catch-all ships as silent redirect per spec, lightweight not-found parked as optional polish · backup-parser defect characterized within the recon rules — pre_sync five-token filenames broke the positional parse, so every pre-sync backup was retention-exempt and accumulated forever; fix parses trailing timestamp tokens, requires the liminal_ prefix (foreign .db files strictly safer than before) · honest-sync-log rider skipped, already fixed in-tree · getBackLabel verified present at BookDetail:1251, queue item dead | Two of seven closing as verified-no-change is the drift check working
- 2026-07-17 | Upload containment (B3/v0.68.0, unplanned spinoff from the B2 reviewer chip): original_name basenamed at intake AND per-path resolved-parent guards — basename kills traversal at the source, guards catch destination-leaf symlink redirects | Belt and suspenders, each covers the other's blind spot
- 2026-07-17 | 'new'-branch collisions refused per-book (error names the files, nothing written, makedirs after pre-flight so rejection leaves no stray folder). Deliberate replacement stays on the review screen's Replace and the Files-section remove — no new overwrite affordance | Silent copy2 overwrite was the last unguarded destructive write
- 2026-07-17 | RULING: a wishlist title that receives files IS a normal title. Link-to-title collisions adopt the S15.2b rename contract (_1 suffix) instead of silently replacing files the title already owns | Same contract everywhere a file lands beside a namesake
- 2026-07-18 | Fix A ratified (B4/v0.69.0): check_familiar_titles includes wishlist rows — root cause of the duplicate factory was WHERE is_tbr=0 categorically excluding folder-less wishlist entries, so uploading a wishlisted book ALWAYS created a duplicate and both cleanup moves lost data. Owned twins win exact ties (ORDER BY is_tbr ASC); add_to_existing gains the Acquire flow's wishlist→owned conversion in the same transaction as the edition insert | Notes and cover survive because the row never changes
- 2026-07-18 | Fix B ratified (B4/v0.69.0): merge carries cover columns source→target — real beats none, custom beats extracted, a custom target never overridden; column re-point only, cover_carried in the response | Existing wishlist/owned duplicate pairs are now losslessly mergeable
- 2026-07-18 | FROZEN EDIT ratified: upload/BookCard.jsx wishlist-match banner — copy only, chrome zone, gradient hex constants untouched (reviewer verified zero hex in the diff) | The negotiable zone doing what it was defined for
- 2026-07-18 | Version/label renumbering: B3=containment (0.68.0), B4=wishlist conversion (0.69.0) per shipped record; the queued renames + silent-failure session runs as B5, v0.70.0 | Shipped CHANGELOG owns the labels; the queue adapts, not the record

#### **==B-batch Drift Check (2026-07-17)==**
- 2026-07-17 | Stale-path scope ruling (B2 gate): stale edition rows — including old-title rows after an upload replace — are BY DESIGN (sync reports vanished files and re-points on the next scan, never deletes; the download endpoint's 404 + "may have moved" detail is the designed surface). The BUG is frontend-only: the no-Web-Share desktop branch downloads via a bare anchor click on the endpoint URL, so the 404 never surfaces — silent failure. B2 fixes the surfacing (fetch-first); downloads.py untouched | Row staleness is the reconciliation model working; the silent branch is GR3
- 2026-07-17 | getBackLabel /sync-results shipped as a v0.57.0 rider (CHANGELOG Changed + Technical, ROADMAP S3 rider) — B2 queue item is stale. B2 recon verifies the case survived the v0.58–0.65 BookDetail rewrites, then drops it; restores only on regression | Queue carried a closed item forward from the pre-0.57.0 ledger
- 2026-07-17 | upload.py:176 logs "Background sync complete" on status='error' results — the S15.3b carry-over assigned to S15.2b was never folded (no CHANGELOG record). Adopted as a B2 rider: two-line status check on files B2 already opens | Orphaned carry-over, smallest possible landing
- 2026-07-17 | Backup-filename parser has no recorded defect anywhere in the vault (name-only in ROADMAP + the batch-structure entry). B2 handles it recon-first with a STOP if no defect is found | An un-specced fix is not a license to invent one
- 2026-07-17 | v0.66.0 riders, all ratified: confirm match case-insensitive + trimmed (mobile autocapitalize) · containment strengthened beyond spec — symlinked-_trash refusal, books-root-equality refusal, and stats refuses whatever empty refuses (counting what you can't delete is a lie) · removal records at logger.warning because NOTHING configures logging app-wide — INFO never reaches the container log (recorded as an app-wide fact, not just a trash-router quirk) · Python-3.11-portable symlink checks (is_dir follow_symlinks kwarg is 3.13+; container is 3.11) · api.js wrappers in-scope by standing convention even when unnamed | S10 two-step deviation sanctioned: the type-forever gate is stronger than a second confirm step

#### **==Batch-3 Decision Sprint (2026-07-16)==**
- 2026-07-16 | Batch structure locked: A1 backend safety (upload-replace containment + import_metadata.py removal) · A2 alert() sweep to zero + strict lint · A3 Settings/menu polish (status-label modal, ThreeDotMenu z-index, Files × ungating). B1 Trash surface · B2 small-fix sweep (desktop stale-path download, backup-filename parser, AddPage skip/defer messages, getBackLabel fold-in) · B3 renames + list-view silent-failure audit. C: lint hardening + microcopy verification | Priority follows the handoff queue; destructive-op guard ships first
- 2026-07-16 | Upload replace: containment guard (borrow trash.py guard logic) AND route the deletion through _trash instead of rmtree | Applies the ratified 2026-07-12 principle — container-side deletes bypass the NAS Recycle Bin; replace was the last op still violating it. Guard + move, not guard alone
- 2026-07-16 | Files section Edit⇄Done DELETED — SUPERSEDES the 2026-07-13 hybrid pattern: per-row remove × always visible, muted (text-muted), 44px target, editions > 1 guard kept, existing confirm modal remains the gate | The mode double-gated an action that already has a two-step confirm, and its state wiring caused two v0.61.0 defects on its own. Press-and-hold rejected: invisible affordance, fights browser long-press, and the gesture already means BookContextMenu in list views
- 2026-07-16 | Section-header affordance convention ratified for the future sweep: pencil icon = opens an editor. Files section keeps no header affordance | Same signal must mean the same thing everywhere; the mode-toggle case no longer exists
- 2026-07-16 | Settings → Trash surface ships WITH [Empty trash]: size + count, type-to-confirm gate (type "forever"), copy states plainly that emptied files skip the NAS recycle bin AND are not covered by backups | The app absorbed the reasons to visit the NAS — a NAS-dependent emptying workflow is a dead workflow; _trash was forgotten multiple times in its first week. This becomes Liminal's only truly irreversible operation and the copy owns that
- 2026-07-16 | Settings backup section gains explicit microcopy: backups cover the library database only, not book files | "Backup" currently over-promises the same way the old Remove Format copy did; rides B1 (same Settings surface)
- 2026-07-16 | Status-label modal: field labels are static canonical defaults, never live-updating to custom values; per-field reset = text "Reset" link right of the label, renders only when value ≠ default | Variant A mockup approved 2026-07-16 — Variant B's icon buttons eat input width and don't say what they undo
- 2026-07-16 | ThreeDotMenu mobile fix: bottom sheet + backdrop stack above the bottom nav | The sheet is modal — nav must not be visible or tappable under it; the padding workaround is fragile across safe-areas
- 2026-07-16 | Duplicates dismiss-pair mechanism parked until after the navigation redesign | Not gating batch 3; nav redesign outranks it
- 2026-07-16 | A1 recon rider: ImportPage.jsx (routed, unlinked, calling three dead import_metadata endpoints) deleted with the router — route + import removed from App.jsx. Obsidian import is REMOVED as a feature; if it ever returns it's a new feature built against the titles schema, not a resurrection | Backend broke at the books→titles rename and nobody noticed — the feature was already gone, the UI just hadn't been told. Also retires 4 of A2's 11 alert() sites
- 2026-07-16 | A1 ratification: out-of-tree replace rejection surfaces as the batch endpoint's per-book error result, not HTTP 400 — a mid-batch 4xx would discard books already landed. Two formerly-silent fall-through-to-new degradations (missing/nonexistent existing_folder) now error explicitly | Nothing silent, nothing discarded; the spec's 400 was wrong for a batch contract
- 2026-07-17 | v0.63.0 riders: cover errors render beside the cover controls and the batch-remove banner lives in the fixed bottom bar — point-of-action visibility over top-banner convention on fullscreen mobile modals, ratified. alert-call lint was already strict since v0.57.0; category reached 0 this session | Banner placement rule for future modals: error renders where the failed action lives
- 2026-07-17 | v0.64.0 RETROACTIVE ratification: DELETE /collections/{id}/books/{title_id} is idempotent — removing a non-member is a success no-op. Mirrors the duplicate-tolerant add side; checklist endpoint's strict 404 deliberately kept (completion toggling requires the row). Decided mid-session on Claude Code's proposal, approved by Marie — flagged as a decisions-first exception, not a precedent | Fixes the batch-remove retry trap at the root; v0.63.0's Try again promise is now keepable
- 2026-07-17 | v0.65.0 riders: ThreeDotMenu sheet + backdrop portal to document.body — first createPortal in the codebase, ratified as THE pattern for escaping ancestor stacking contexts (root cause was UnifiedNavBar's sticky z-20 context, not the z value). Reset persists through the save-on-blur path (persistLabel) since the modal has no Save button. !text-text-muted override on the Files × accepted as minimal-in-scope; IconButton muted variant is the conforming fix, queued | Portaled overlays need their own outside-tap containment (sheetRef) — subtree checks stop working by definition

#### **==S16 Status Knot + BookDetail IA Decision Sprint (2026-07-14)==**
- 2026-07-14 | SOURCE OF TRUTH: sessions-canonical (Model B). titles.status / date_finished / rating stay as columns but become a materialized projection recomputed on every session write — never written directly by UI paths | Reads were already session-derived (rating avg, History tab); only writes drifted. No schema change, database.py stays frozen
- 2026-07-14 | Projection rule: title status = session with latest date_started, tie broken by higher id (ORDER BY date_started DESC, id DESC LIMIT 1); rating = avg of session ratings; date_finished = latest closed session's end date | Day-granular dates make same-day DNF-then-restart a real tie; insertion order resolves it
- 2026-07-14 | date_finished clobber bug dissolves rather than gets fixed — status changes write sessions, projection recomputes, the null-passing handlers die | Third-strike bug (S3, S10, this sprint); root cause was the write path, not the handler
- 2026-07-14 | Completion status decoupled from reading status: edit-modal "Completion Status" is publication state (property of the work), severed from reader progress everywhere it's wired | Most-reported item across all docs; a WIP fic can be fully read
- 2026-07-14 | BookDetail action surface: ReadingStatusCard and ChangeStatusModal DELETED from BookDetail; replaced by inline 4-state segmented toggle (Unread / Reading / Finished / DNF via useStatusLabels) below the Download button. Interactive mockup approved 2026-07-14 | One tap replaces open-modal→select→apply; pill becomes live projection of sessions — fixes "pill not connected to History"
- 2026-07-14 | Toggle layout: CSS grid repeat(auto-fit, minmax()) — wraps to 2×2 automatically when custom labels exceed single-row width. Never truncate, never ellipsize, no JS measuring | Custom labels ("Not started" / "In progress" / "Abandoned") are user-configurable and must render whole
- 2026-07-14 | Download-triggered transition: Download while status=Unread offers inline "Start reading?" — accept creates an In Progress session (today + downloaded format). Decline does nothing. Never fires on other statuses | The S14-deferred item; sessions-canonical makes it a clean write, not a drift vector
- 2026-07-14 | Download button loses the format suffix — reads "Download" only | Picker already announces the default
- 2026-07-14 | Finished state renders inline capture (date + rating) in the action surface; MarkFinishedModal's job collapses into the toggle + session write. Modals survive as shared components for list-view long-press (BookContextMenu) but write through the session-writing path | BookDetail-only kill; list views keep working and become drift-safe via the backend rewire
- 2026-07-14 | 3-dot menu final: Edit · Change Cover · Merge · Rescan Metadata · Delete Title. Add Reading Session → History tab + button (replaces pencil); Add to Collection → deleted (Collections section owns it) | Corrects Decisions 2026-07-12 line: "Extract Cover" was never a menu item — it lives inside the Change Cover modal
- 2026-07-14 | Details / Notes / History tabs SURVIVE — tabs are parallel destinations, not disclosure; collapse hid content already on screen, tabs separate activities | If daily use post-toggle disagrees, that's a future subtractive session with evidence
- 2026-07-14 | Details section order: Download → status toggle → About → Tags → Metadata → Collections → Files — current order minus the dead card | Item 5 resolves to "no change beyond the removal"
- 2026-07-14 | Download picker size-removal + EPUB-default-first: verified already shipped, item closed | Screenshots 2026-07-14 confirm
- 2026-07-14 | Remove-format confirm honesty fix: suppress "The file moves to the trash folder." when file_size === null (stale, client-detectable) — backend deliberately skips the move there; shared-path case handled per Session 3 recon | Same class as the merge modal's old "cannot be undone" lie
- 2026-07-14 | Riders: History tab sessions sort newest-first (currently insertion order); Times Read counts closed sessions only (currently counts the open Reading session) | Both spotted in 2026-07-14 screenshots
- 2026-07-15 | B1 recon rulings: projection already existed as sync_title_from_sessions in frozen database.py (ordered by session_number — backfilled reads stole status; date_finished clobber lived there, not only in handlers). RATIFIED: in-place edit, function body only — new ordering, closed-sessions-only date_finished, single-transaction execution. date_started joins the projection (winning session's value). merge_titles gains a target recompute after existing writes (staleness bug, records-only). import_metadata.py parked to batch-3: unmount/delete, writes to pre-Phase-5 books table, dead-broken | Frozen-file edit beats stranding dead code in a frozen file
- 2026-07-15 | Unread semantics ratified from B1 phone testing: Unread deletes open (in_progress) sessions ONLY — dnf and finished records are history and survive. Toggle never silently destroys a dated record; true reset is deliberate via History tab session delete. B2 renders returned status + microcopy for both snap-back cases | Destructive deletes don't hide inside a status change
- 2026-07-15 | Toggle wrap mechanics corrected in B2: CSS auto-fit is viewport-driven, not content-driven — long custom labels wrap to two lines within their segment; 2×2 is strictly the narrow-viewport (<~360px) fallback. No truncation either way | min 76px, ratified over JS measurement
- 2026-07-15 | B3 rulings: Times Read counts finished sessions only — DNF is closed but not Done; the projection's own finished-only closed-date logic is the precedent. average_rating deliberately still spans DNF ratings | Ratified over the code's count-everything, which was the bug
- 2026-07-15 | Remove-format shared-path honesty: Option A (frontend-only) — trash sentence suppressed on file_size null OR within-title path share; shared case renders "The file stays — another format still uses it." Cross-title sharing accepted as uncovered edge (abnormal state; sync ingests per folder, merge trashes source folders) — if it ever surfaces, fix is Option B: file_shared flag on EditionSummary in get_book | Kept a per-edition query out of the get_book hot path for a state the system doesn't produce
- 2026-07-15 | SUPERSEDES the 2026-07-15 toggle-wrap entry: final B2 mechanics are content-driven, not viewport-driven — auto-fit DELETED (CSS forbids intrinsic tracks in auto-repeat; its narrow-viewport 2×2 fired only below ~360px, dead in practice). Rule: all labels ≤11 chars → grid-cols-4 single row; ANY label >11 → grid-cols-2 true 2×2. Threshold 11 because the shipped default "In Progress" is 11 chars (spec's 10 would render out-of-box 2×2). Overlong labels wrap in-segment, never truncate, no JS measurement | Phone-tested; the intermediate 76px form never shipped as final

#### **==2026-07-14==**
- 2026-07-14 | Kill all collapse and truncation on BookDetail — Files, Metadata, Tags all render fully expanded, always. No chevrons, no View more fade | Found in daily use: disclosure hides content and costs taps on a screen where scrolling is free. Accepted consequence: long summaries mean a long scroll. If that bites, line-clamp the summary alone — do not reintroduce disclosure. The Files section's Edit/Done toggle survives; it gates destructive actions, not content
- 2026-07-14 | Add format and Add files are one action split by an implementation detail (record-only vs. upload). Unify into one entry point where the chosen format drives the form: file-backed formats prompt for a file, physical/audio ask for acquired date only. Add files dies | The user's question is the same either way — I have this book in another format. The app currently makes them know which internal path they need before they can answer. Locking the principle; exact shape is the IA sprint's job
- 2026-07-14 | Public docs: describe the SHAPE of the deployment, never the ADDRESS of it. Generic architecture (Docker on a NAS, SQLite, WebDAV, Ollama) stays and makes the repo usable; instance-identifying detail (IPs, hostnames, ports, host paths, remote access, backup locations) never appears | The old rule forbade all NAS/infrastructure mention, which is unenforceable for a self-hosted NAS app — the doc and the rule disagreed, so the rule lost

#### **==2026-07-13==**
- - 2026-07-13 | ui/CollapsibleSection is a "View more" truncation fade, not a disclosure component — no chevron, no header-action slot, no expanded-state callback. Files section uses BookDetail's own section pattern with local collapse state and an h2/button aria-expanded header instead | Ratified. Extending it mid-session was out of scope. The name lies; rename or build a real disclosure primitive before another section needs one
- 2026-07-13 | design-lint's alert() rule ships red at 13 known sites — set a baseline count so the rule fails on INCREASE, not on existence, until batch 3 drives it to zero | A category that always fails is a category nobody reads, and a 14th alert() would slip through invisibly
- 2026-07-13 | Remove-format confirm copy currently over-promises: "the file moves to the trash folder" is false for shared and stale paths, both of which delete records-only. Stale is client-detectable via file_size === null | Same class as the merge modal's "cannot be undone" lie. Batch 3, tagged honesty fix, not microcopy polish
- 2026-07-13 | Files section: hybrid interaction pattern (mockup-approved). Placed below Collections. Add format / Add files always visible in section footer; per-row remove (×) gated behind an Edit/Done toggle in the section header. Edit link renders only when expanded and editions > 1; edit mode resets on collapse | Constructive actions must advertise themselves; destructive ones cost one deliberate tap. Refines the 07-12 "actions move to Files section" decision — this is the pattern, don't re-litigate
- 2026-07-13 | Remove Format trashes the single file via new move_file_to_trash() sibling in trash.py (same guards, file-only, directory raises); files-first contract; shared-file_path guard skips the trash move; fileless editions delete records-only | move_to_trash's directory-only raise stays a deliberate contract for delete-title; one function that accepts both eventually trashes the wrong thing
- 2026-07-13 | EditionSummary gains file_size, stat'd server-side at request time in row_to_title_detail, null for fileless/stale paths | Locked 07-12: stat-at-load can't go stale

#### **==2026-07-12, merge change of heart==**
- **Merge file-motion — REVERSED 2026-07-12.** The batch-2 sprint decided B's formats that A lacks get their file moved into A's folder with the edition re-pointed. Reversed: merge never moves files between folders. All of B's editions are dropped; B's folder goes to trash with its files. A keeps only what it had. Rationale: scenario hasn't occurred in 1+ year; cross-folder file motion is the highest-risk op in the app; any format lost this way sits in _trash and re-adds via Add Format in seconds.
- **Merge editions bug (found in review).** Merge re-pointed non-duplicate editions via UPDATE title_id without updating folder_path/file_path, producing a title owning a row that points into a foreign folder. Never hit in practice — every real merge took the duplicate-format branch. Made unrepresentable by the reversal above.

#### **==2026-07-12, Batch 2 Session 1 ratifications==**
- **check_duplicates skips `_trash`** — RATIFIED. Not defensive; load-bearing. Without it, deleting a work and re-uploading it is blocked as a duplicate by its own trashed file.
- **ThreeDotMenu danger flag** — RATIFIED, additive prop, default false. No existing menu changes appearance.
- **Modal vs inline for menu-launched deletes** — RATIFIED, and generalized. DESIGN_SYSTEM §4 amended rather than granting a one-off exception: Sessions 2 and 3 both need this same pattern (merge, remove format). Exception-per-session would relitigate it twice more.
- **Step 1 buttons corrected** [Not Now] → [Keep]. "Not Now" is a deferral, not a safe outcome. Step 2 was already correct.

#### **==Batch-2 Decision Sprint (2026-07-12)==**
- 2026-07-12 | UI-initiated deletion ships: title delete removes the DB row AND the NAS folder, behind a two-step confirmation. Sync still never deletes — this is user-initiated only | Marie has been doing filesystem surgery in File Station because the app was too polite to do it; the no-delete policy was friction, not safety
- 2026-07-12 | Deletion is a MOVE to `/books/_trash/`, never rmtree. Sync ignores `_trash/`. Marie empties it manually via File Station (which routes to #recycle for another 30 days). Two-step confirmation still applies | Synology's Recycle Bin does not intercept container-side filesystem deletes, so rmtree would be genuinely permanent. Move costs the same line of code and buys a real undo window — a lesson priced at 618 titles
- 2026-07-12 | Download picker shows NO file size. Size moves to the BookDetail Files footer instead: FORMAT · filename · size, stat'd at load | Size is reference info, not decision info — nobody aborts a download over 4 MB. Stat-at-load can't go stale; store-at-sync can
- 2026-07-12 | Format actions leave the 3-dot menu entirely: Add Format, Add Files, and Remove Format become affordances on the BookDetail Files section | Actions belong next to the thing they act on; the overflow menu should hold only actions with no home section
- 2026-07-12 | NEW SPRINT ITEM — BookDetail 3-dot menu audit: same treatment for Collections (→ Collections section) and reading-session actions (→ ReadingStatusCard). Menu retains only homeless actions (Delete Title, Rescan Metadata, Extract Cover). NOTE: the reading-session move collides with the S16 Status Knot (ReadingStatusCard source-of-truth is unresolved) — the audit must either stop short of that move or S16 gets pulled forward | The 3-dot menu became a dumping ground
- 2026-07-12 | Merge actually merges files: formats present in the losing title but not the kept one are MOVED into the kept title's folder and their edition records re-pointed; duplicate formats are dropped; the emptied folder is then deleted (same delete mechanism as above, two-step confirm). Confirmation states files will be deleted | "Delete the loser's folder" would silently destroy a format the kept title lacks — the exact case a user would be furious to lose. Making the merge real fixes the sync re-ingest bug AND makes the word honest

#### **==P1 Overwrite Fix Ratifications (2026-07-12)==**
- 2026-07-12 | Fix lives at the shared existing-title write path, not behind the full flag — recon proved incremental sync hits the same UPDATE via relocations, conflict folders, and post-upload background sync | It was never a full-sync bug; the Settings button just fired it 2,170 times at once
- 2026-07-12 | Empty test includes '[]' for JSON-list columns (authors/tags); sync skips the UPDATE entirely when nothing needs filling (no updated_at churn) | An empty JSON array is empty, and a no-op sync should leave no fingerprints
- 2026-07-12 | 618 damaged titles NOT restored — moving forward from current state | Marie's call: too many changes since the last backup to make a revert worth it

#### **==S15.3b Session Ratifications (2026-07-11)==**
- 2026-07-11 | formatTimeAgo extracted to frontend/src/utils/formatTimeAgo.js (verbatim lift from Settings) — shared by Settings and SyncResultsPage | Two surfaces, one helper; utils/ is where it belongs
- 2026-07-11 | Sync result detail lists capped at 200 entries per type, counters uncapped, view says "…and N more" | Settings-row JSON blob shouldn't grow unbounded; nobody triages 200+ findings from a list anyway

#### **==S15.3a Session Ratifications (2026-07-11)==**
- 2026-07-11 | Toast entrance animation stays deleted; tailwindcss-animate NOT installed | The classes emitted zero CSS since the component was written — no visual change to preserve, and calm UX means minimal animation by default
- 2026-07-11 | Confirmation dismissal label: voice doc's never-Cancel rule wins over the S15.3a prompt's "Cancel"; reconciled in S15.3b using the MICROCOPY_LIBRARY-approved label | The prompt locked a voice violation; prompts don't outrank the voice doc

#### **==S15.3 Scope Lock (2026-07-11)==**
- 2026-07-11 | S15.3 ships BEFORE the Vow/TheDae NAS cleanup — the results view IS the triage list; conflicts/skips get cleaned using the new surface, closing Pipeline #1 | Doing filesystem triage from container logs when the log-replacement feature is one prompt away is backwards
- 2026-07-11 | S15.3b results view is PERSISTENT and ACTIONABLE: last-sync summary lives on the Settings page (stored SyncResult), shown immediately after a manual sync via the same component; every warning links to its title (missing file → open title to delete/merge; conflict → show both folder paths) | A dismissed modal sends you back to container logs; TheDae showed that "reported, never deleted" is only painful when the report is a dead end

#### **==S15 Session 3 follow-up (2026-07-10)==**
- 2026-07-10 | Orphan semantics for multi-folder titles: a title is orphaned only when NONE of its folder-backed editions' folders still exists — one live folder keeps it un-orphaned | Multi-folder titles are legal since S15 format-aware sync (live format conflicts); the old per-edition rule flip-flopped such titles between orphaned/recovered every full sync (reproduced by S15-S3 adversarial verification). Single-folder titles behave exactly as before
- 2026-07-10 | SUPERSEDES chip decision above: BookDetail top chips stay COARSE — all storage-format editions collapse to one "Ebook" chip (deduped); granular formats render only in the Files footer, download picker, and Remove Format picker | Live data showed mixed-taxonomy chips (EPUB beside Audiobook) and post-backfill would show 5 chips per multi-file title; mockup approved pre-backfill couldn't show this
- - 2026-07-10 | Full sync gets a Settings-page action with a confirmation step; post-sync results view lists conflicts, duplicate skips, and missing files in plain language (S15.3) | Deploy revealed the API supports ?full=true but no UI can send it; conflict investigation currently requires container logs

#### **==S15 Decision Sprint (2026-07-10)==**
- 2026-07-10 | Two-tier format model: dropdowns keep coarse groups (ebook/physical/audiobook/web); file-backed editions store extension-derived storage formats (epub/pdf/mobi/azw3/azw/html) | Humans pick groups, files know their own format — no dropdown gains options
- 2026-07-10 | Reading session format untouched — reading_sessions.format is a separate column with its own validation; keeps coarse list | Sessions don't care which file you read
- 2026-07-10 | 'ebook' survives as a legal editions.format value meaning "ebook, no file" — metadata-only Add Format writes it; also the migration fallback | Removing it costs code for zero benefit
- 2026-07-10 | Shared EBOOK_FORMATS constant, defined once per side, replaces inline lists before feature work | Recon found 10+ hardcoded format lists, zero shared constants
- 2026-07-10 | HTML is a first-class downloadable storage format | Marie stores .html copies (verified on NAS); text format, shareable; MEDIA_TYPES gains text/html
- 2026-07-10 | Download guard becomes format IN EBOOK_FORMATS AND file_path (downloads.py:47 + BookDetail downloadableEditions) | The two confirmed gates
- 2026-07-10 | DB records ONE file per title today even when the folder holds five — upload records first-moved-file only, sync records epub-preferred only. Verified: 2,173 editions = 2,173 folders, screenshots show 5-file folders | Root cause of "where are my mobi/azw3/html files"; files were never lost, never recorded
- 2026-07-10 | Migration is relabel + backfill: (1) script re-labels recorded editions by extension, file-less/unknown → 'ebook'; (2) format-aware full sync registers sibling files as new editions | Relabel alone leaves the picker dormant for the existing library
- 2026-07-10 | Full sync goes format-aware: one edition per recognized file per folder, matched by (folder_path, storage format); replaces the (title_id,'ebook') lookup | Single discovery engine — no separate backfill script; also kills the recon §7 duplicate-edition bug
- 2026-07-10 | Same storage format twice in one folder → deterministic pick (alphabetical), skip + report in sync summary | One-edition-per-format constraint holds; locked answer unchanged
- 2026-07-10 | Generic 'ebook' (file-less) + specific storage formats on one title = legal pair | Unique index allows it; forbidding costs merge logic
- 2026-07-10 | library.db backup before migration AND before first format-aware full sync; database.py edits flag-and-ratify | Backfill creates thousands of rows — verify counts against expectation after
- 2026-07-10 | Add-file flow: "Add file" in BookDetail 3-dot menu → existing /add?mode=upload&linkTo={id}; link-to-title learns owned titles + duplicate-storage-format inline error. Upload must record EVERY moved file as its own edition, not first-only | Fixes the amnesia at the source for future adds
- 2026-07-10 | BookDetail file-info footer lists each file-backed edition as "FORMAT — filename" alongside folder path | At-a-glance file truth; post-backfill these titles show all five
- 2026-07-10 | Fold-in fixes ride S15: unguarded edition insert (upload.py:442), unvalidated format on POST /titles, desktop stale-path download bug | All on files S15 already opens

#### **==S13 Completion Decisions (2026-07-09)==**
- 2026-07-09 | 10.1 Download: button label is "Download," not "Read" | No in-app ereader — every format opens elsewhere. Voice doc button-only test: "Read" promises reading Liminal can't deliver; "Download" names the actual action
- 2026-07-09 | 10.1 Download: button never changes reading status in S14 | Downloading is not "started reading." Keeps the feature off the Status Knot; any auto-transition is S15 knot-sprint work
- 2026-07-09 | 10.1 Download: multiple text editions → picker (default EPUB); single edition → no picker | Skip the choice when there's nothing to choose
- 2026-07-09 | 10.1 Download: button hidden for wishlist entries and for owned books whose only downloadable format is audio | No file, or no text file, means nothing this button can serve; audiobooks are a separate future action
- 2026-07-09 | 10.1 Download: mixed text+audio books show the button; picker lists text editions only, audio ignored | This button serves reading formats; audiobook handling parked
- 2026-07-09 | 10.1 Download: Android uses Web Share API (share sheet → Moon Reader); desktop/unsupported browsers direct-download to downloads folder | Matches the 95%-mobile primary path without breaking desktop
- 2026-07-09 | 10.1 Download: button placed full-width above ReadingStatusCard, not inside it | Decouples the button's fate from the card's — the card is a likely S15 knot casualty
- 2026-07-09 | 10.1 Download: loading state + success/error toast, no silent failures | CLAUDE.md no-silent-failure rule
- 2026-07-09 | 10.1 Download: no file size in the picker | EditionSummary carries no size; not worth a backend field for a rarely-seen picker. Not a fast-follow — parked

#### **==S13 Decision Sprint (2026-07-08)==** 
All six queue items locked. S13 ships four artifacts: design-lint script, DESIGN_SYSTEM.md (new), ARCHITECTURE.md (rewrite), code-reviewer subagent.
- Amendment (2026-07-08): CURSOR_PROMPT_GUIDE.md stays archived — Cursor-era methodology, not resurrected or renamed. Its one live obligation (run design-lint around UI work) moves to CLAUDE.md as a standing agent rule. Prompt conventions live in the Claude project instructions; no standalone prompt guide.
- Guardrail mechanics: pre-commit hook = **warn-and-allow**, never hard-block — commits happen in Marie's Terminal with no agent session running; a hard block at that moment kills momentum with no fixer available. Enforcement surface = the committed markdown report (living scoreboard) + manual `npm run design-lint` + CURSOR_PROMPT_GUIDE note to run before UI prompts. Threshold strict (zero on the 408 audit scope); frozen files allowlisted by filename; inline `// design-lint-ignore` for one-offs.
- Lint owns everything greppable, including anti-pattern strings: `window.confirm(`, "Abandoned"/"Did Not Finish" literals, indigo classes, `font-bold` headings, cascade-flip pairing, S12 audit patterns. Raw `<button>` outside ui/ = report-only category until the post-S13 conversion backlog clears, then strict.
- Code-reviewer subagent (`.claude/agents/code-reviewer.md`) scope = three judgment checks only: (1) frozen-file diff detection — reads the six names from CLAUDE.md at review time, never from memory; flags with the file's "why frozen" line and requires a stated exception; (2) scope drift vs. the prompt's stated scope + out-of-scope list; (3) pattern conformance spot-check for un-greppable DESIGN_SYSTEM rules (token-in-wrong-context, modal structure, inline confirmation, silent-failure smells) — cites DESIGN_SYSTEM.md sections, never restates them. Runs at end of every Claude Code session, before the deploy manifest; findings append to the session report beside the lint scoreboard.
- No-duplication rule: rules live in exactly one file. Subagent file = review checklist + pointers (CLAUDE.md for frozen, DESIGN_SYSTEM.md for patterns). Sole sanctioned copy: the lint allowlist hardcodes the six frozen filenames (scripts grep names), with a comment stamping CLAUDE.md as canonical.
- DESIGN_SYSTEM.md scope = option (b): canonical rules + per-component API docs. No screenshots — /dev/components is the live gallery. Precedence line at top: CLAUDE.md wins on frozen files, tailwind.config.js wins on token values, this doc wins on usage patterns.
- **text-h1 dies.** Zero usages app-wide; a documented-but-unused token is Cursor bait. Delete the fontSize key in S13; DESIGN_SYSTEM.md typography section notes it as intentionally absent (largest heading is h2, the wordmark). Re-adding later is a one-liner.
- Cross-link strategy confirmed: DESIGN_SYSTEM = what, PHILOSOPHY = why, VOICE = how to write. Cross-link both ways; never copy token values (config + liminal-tokens-warm.css are the source).
- Component inventory: all 14 shared components, template-driven — purpose · variants/states · required props · when to use · when NOT to · common mistakes · **frozen behaviors as a required heading** ("None" where empty; optional bullets get skipped, required slots don't). Seeds: SegmentedControl 11px label lock, Modal ✕-right + Cancel/Primary footer, BookCard variant API.
- ARCHITECTURE.md rewrite scope confirmed: cut Phases 1–6 history (git has it); keep system overview, stack + versions, file structure, current schema (hand-written, "if this contradicts database.py, database.py wins"), API surface, three lifecycle diagrams (book / session / wishlist→library), frontend architecture, deployment workflow, frozen subsystems, cross-links, 11pm-debug section (ports, env vars, logs, DB inspection, clean reset).
- **ARCHITECTURE.md is git-tracked and public** — same rule as ROADMAP: no NAS paths, IPs, share names, or home-network details. Deploy workflow written generic ("copy changed files to the container volume"). Debug section uses container-relative paths only.
- Schema section gets a one-line Status Knot pointer (Title status fields + ReadingSession overlap = known drift, redesign parked, see Open Questions) so the doc doesn't canonize the current mess as intended design.

#### **==Token Migration Session Close (2026-07-06)==** 
Shipped as v0.49.0.
- upload/BookCard.jsx one-line color append ratified as a sanctioned frozen-file exception — the @layer deletion would have silently recolored its one unpaired site; rendering preserved, file otherwise untouched. Frozen rule stands: exceptions get flagged, never silent. S13 code-reviewer subagent gets frozen-file detection, sourced from CLAUDE.md, never from memory.
- Baseline reconciliation: S12 audit excluded 3 frontend frozen files by design (85 = 88 − 3). 408 = audit-scope count = S13 lint zero-target (lint allowlists frozen files); 429 = true repo-wide count incl. upload/BookCard's 21. Not an audit bug.
- text-h1 has zero usages app-wide; fontSize key kept, JIT drops the unused utility. DESIGN_SYSTEM.md decides whether h1 lives or dies in S13.
- SegmentedControl text-[0.6875rem] is NOT untracked drift — it's the locked 11px segmented-control label size (Fix Session 5, 2026-04-13). S13 lint ignore-list seed.

#### **==Frozen Files: Policy Clarification + CLAUDE.md Relocation (2026-07-06)==** 
Prompted by the v0.49.0 exception. Not a loosening — documenting what frozen already meant.

- Frozen ≠ untouchable. Frozen = any edit requires justification against the file's stated risk, a flag at edit time, and Marie's ratification (logged here + CHANGELOG). Silent edits stay violations even when correct — unflagged-correct is indistinguishable from unflagged-wrong.
- The bright line stays exactly six names (lint greps names, not judgment). Planned thaws go through decision sprints with scoped constraints, then re-freeze. Status Knot is expected to open database.py this way.
- CLAUDE.md now carries a per-file "why frozen / danger zone" line for all six.
- CLAUDE.md relocated to repo root, gitignored (2026-07-06) — it had lived as `CLAUDE.md .txt` in the vault, where Claude Code never auto-loads it; root cause of the frozen-list blindness this session. Untracked by design: public repo, and `git add .` paste blocks must stay leak-proof. Only copy = repo root on NAS dev share (Synology snapshots are its backup); vault copy deleted.
- Liminal SKILL.md corrected upstream in claude.ai (17 fixes: canonical six, sync.py-not-frozen corrective, library.db, typography rewritten to v0.49.0 truth, ghost components removed, version stamp + "CLAUDE.md wins" deferral clause).
- Open erratum, next repo touch: CHANGELOG 0.47.1 line 299 says "sync.py — frozen"; add a one-line correction (edit-in-place permits errata; not flattening).

#### **==S12 Close: Batch 2–3 Judgment Calls (2026-07-06)==** Ratified post-hoc; all shipped in v0.48.0.

- Typography emphasis precedent: stat values → text-h4; inline emphasis inside body-size text → text-label (never h4 mid-sentence). BookDetail 7/3 split.
- Ratings live on reading sessions: the rating pill opens the session editor on the latest session (Add Session fallback). No standalone rating surface; the never-landed S3 popup code was deleted as dead.
- Series landing = group view → fixed grid, exempt from grid-columns setting, mirroring Collections.
- App version constant = FastAPI version= in backend/main.py (was stale at 0.10.0; only in-code candidate). package.json untouched (lockfile desync risk without npm).
- Token migration session scope addition: define action-warning-hover in config (Button warning variant currently improvises with /85 opacity).
- selectedRating in BookDetail is write-only post-cleanup — left in place; belongs to the parked Status Knot sprint.
- S13 lint ignore-list seeds (logged in ROADMAP): BottomNav emoji sizing, SeriesCard inline fontWeight, CollectionsTab + Series fixed grids, nested-span-in-label-prop false positive.

#### **==S12 Triage Confirmations (2026-07-05)==** 
Sweep report: FRONTEND_AUDIT_S12.md @ 6b9789c. Baseline table = S13 lint targets.

- §1 token-class migration (394 usages): parked out of S12 batches. Strategy locked: move 8 typography defs to tailwind.config fontSize (names unchanged), append mapped color at unpaired sites only, delete tokens.css @layer components block. Own short session between S12 and S13 — gives S13 a zero-target instead of a 394 freeze.
- Category B (182 raw elements): parked post-S13. Per-file conversions, largest first (BookDetail 30, Library 15), done under lint + DESIGN_SYSTEM guardrails. Exception: modal footer buttons forced by Batch 1's ui/Modal conversions convert now.
- I2 CollectionsTab grid: closed — Collections is intentionally exempt from the grid setting (prior decision). Lint-ignore.
- ReadingStatusCard labels: route through getLabel (also fixes P1 "DNF renders as Not Started").
- D stat-value pattern: font-semibold → text-h4.
- H2: BookDetail Abandoned badge → status-dnf, matching ImportPage.
- S11 regression defects (rating tappable, series sort, Series grid leak, MarkFinished validation): folded into S12 as Batch 3.
- Batch structure: B1 straggler files (file-based) · B2 scattered mechanical (category-based) · B3 regression defects. All ship as 0.48.0.
- AnalyzingModal: parked (dismiss-semantics decision). .glass-panel deletion: S13.

#### **==Session 12 Decision Sprint: 10.0E Design System Consolidation (2026-07-05)==** 
Gates the S12 consistency sweep. All nine sprint questions answered; sweep prompt unblocked.

- Phase numbering: split. S11 closes 10.0D (fix sessions); S12/S13 become **10.0E "Design System Consolidation."** The work changes character — fix → consolidate → enforce — and deserves its own label.
- Versioning: S12 ships as **0.48.0** (phase milestone = minor bump). 0.47.x stays reserved for regression hotfixes.
- DESIGN_SYSTEM.md location: **root/docs**, alongside DESIGN_PHILOSOPHY and VOICE_AND_TONE, so the coding agent sees all three together.
- Frozen file list (untouched by sweep, ignored by lint): `GradientCover.jsx`, `MosaicCover.jsx`, `upload/BookCard.jsx`, `backend/services/covers.py`, `backend/services/metadata.py`, `backend/database.py`. These six, no additions.
- Sweep categories: ship the full flag list — hardcoded colors (`text/bg/border-gray-*`, `text-white`, `bg-zinc/slate-*`, raw hex, default indigo/red/green/blue/yellow); raw `<button>`/`<input>`/`<select>`/`<textarea>` outside `components/ui/`; color/typography className overrides on shared components; typography drift (`text-xl/2xl/3xl`, `font-bold/semibold` outside ui/); pages missing `UnifiedNavBar`; modals not on shared `<Modal>`; fields not in `<FormField>`; philosophy drift (red on DNF/set-aside, "Abandoned" in copy, any indigo); missing `useStatusLabels`/`useGridColumns` adoption; **two-token-systems migration** (every tokens.css component-layer color usage flagged for migration to config utilities — root cause of the 0.47.2 BookDetail bug). Sweep must produce **hard counts**; those counts are the S13 lint's zero-target baseline.
- Output format: same as `FRONTEND_AUDIT_2026.md` (counts + top offenders + adoption matrices) for a clean before/after comparison against the January audit.
- Sweep ordering: single unified pass, structured output. No per-category re-sweeps.
- Triage: three buckets — **Fix now** (mechanical, mapping-table-able), **Park** (needs UX/layout decision → Roadmap), **Lint-enforced** (S13 catches it, no manual fix). The fix-now/park line: "can the agent do it with a find/replace mapping table?"
- BookDetail drift: don't pre-decide. It was the worst offender (211 colors in Jan) and mixes both token systems — sweep counts decide whether it gets a dedicated re-conversion prompt or folds into the unified cleanup.
- AuthorDetail returnUrl (C6 flag): **verified fixed.** All three entry points (BookDetail →, SeriesDetail →, AuthorsList →) return to the correct previous screen. Closed; out of S12 scope.
- Drawer pattern: `FilterDrawer` is the sole survivor post-S7. One drawer = a component, not a pattern. DESIGN_SYSTEM.md documents it as a one-off, no pattern section.
- Cleanup batching: fix-now items split **by category** (colors / buttons / forms / typography) so each batch verifies independently.
- Verification: spot-check. Thorough test of converted screens + quick adjacent-screen sanity check. Sweep is mechanical — no full regression pass.

#### **==Open Questions Triage (2026-04-14)==**
- Wishlist naming: keep "Wishlist." Researched book tracking community terminology (Goodreads, StoryGraph, Bookly, reddit). "Wishlist" and "TBR" are the two dominant terms but mean different things. In Liminal's context — where every book in the library is already "to be read" — "Wishlist" accurately means books to acquire. No rename.
- Wishlist location: move out of Library tab. Currently lives inside Library, which implies owned content. Wishlist items aren't owned — the container contradicts the contents. Exact placement deferred to navigation redesign (parked as nav redesign input in Open Questions).
- Half-star ratings: implement. 0.5 increments, stored as integer × 2 in SQLite (e.g., 7 = 3.5 stars) to avoid floating point issues. Tap-on-half interaction for mobile. Half-star icon (filled left half) in all display contexts. Not yet scheduled.
- Collection detail trailing line: remove the horizontal divider below the last list item in list view. CSS `:last-child` border removal. Bundle into any nearby session.
- Loading animation: Breathing L. Italic serif L (Libre Baskerville, teal `#5e8a8a`) pulses gently (opacity 0.4→1, subtle scale, 2.4s cycle). Random phrase fades in/out below (Source Sans 3 weight 300, `text-secondary`, 4s rotation). Two contexts: inline loading (centered in content area) and app launch splash (full-screen overlay). Phrase pool — primary (warm/playful): "Brewing something good", "Just a breath", "Hang on, reshelfing", "Good things take a second". Secondary (atmospheric): "One moment", "Settling in", "Almost there", "A quiet threshold", "On the way". Mockup: `liminal-loading-mockup.html` Options A + D.
- Fiction epub cover extraction: not a bug. Extract button only appears when an epub file exists. "A Darker Shade of Magic" missing the button because it's tagged as audiobook-only. If a title is missing the button unexpectedly, check format metadata — data fix, not code fix.
- On hold pending Session 3 results: list view font sizing, context menu "update state" label/icon for finished books, series page author name linking.

#### **==Fix Session 4: Edit Modal Reorganization (2026-04-13)==** 
Findings addressed: UF-25, UF-26, UF-27, G3-02, G3-03
- Kill tabs entirely. Single scrollable form with section dividers. NNG form design guidance: show all fields on one page when possible. Tabs violate H6 (recognition over recall) — user had to remember which fields were behind which tab name, causing a scavenger hunt on every visit. Scrolling is the native mobile pattern; tabs are a desktop complexity-management tool. Solves UF-25 (tabs not noticed), UF-26 (Category/Tags on different tabs), and UF-27 (misleading tab names) by eliminating the root cause.
- Section structure with labeled dividers: (1) Identity — Title, Author(s), Series + Number, Year, Source URL. (2) "Classification" divider — Category (segmented control), Tags, Pairing Type, Summary/Why?. (3) "Fandom Details" divider (only when category = FanFiction) — Fandom, Ships, Completion Status, Content Rating, Warnings. Fandom section gets a subtle teal-tinted background to visually group it.
- Non-fanfic: ~9 fields, ~2 screen heights. Fanfic: ~14 fields, ~3-4 screen heights. Both comfortable single-scroll forms.
- Session 3 constraint update: category pill on BookDetail opens UnifiedEditModal — no tab target needed anymore since there are no tabs. Modal opens, user scrolls to Category in the Classification section.
- Scroll reset (G3-02): reset scrollTop = 0 on modal body when modal opens. Simple useEffect on isOpen. No tab-change reset needed since there are no tabs.
- Unsaved changes guard (G3-03): dirty flag via JSON.stringify(formData) !== JSON.stringify(initialFormData). On Cancel/✕/backdrop when dirty: form content replaced with centered confirmation view. Copy: "Discard unsaved changes?" Buttons: "Keep Editing" (ghost) / "Discard" (danger). User sees confirmation regardless of scroll position because it replaces the form entirely. "Keep Editing" restores form with scroll position preserved. Matches Cancel Upload pattern (G7-07 positive finding). NNG H5 (error prevention).
- initialFormData ref updates after successful save (prevents false dirty state if user saves then continues editing).

#### **==Fix Session 5: Form Input Guards (2026-04-13)==** 
Findings addressed: UF-11, UF-07, UF-12, UF-03, G3-07
- Author field: kill ChipInput for authors. Replace with plain text input using comma separation everywhere (WishlistForm, ManualEntryForm). The edit modal already uses this pattern. NNG H2 (match real world) — "Author A, Author B" is how authors are listed on book covers, in catalogs, in citations. ChipInput required learning a non-standard interaction (Enter to commit) with silent data loss as punishment. One pattern everywhere now. Tags keep ChipInput (genuinely high-cardinality field with autocomplete value).
- Start date default (UF-07): new reading sessions default date_started to today (new Date().toISOString().split('T')[0]). Calendar picker remains for backdating. Common case = zero taps. NNG H7 (flexibility).
- Format default: new reading sessions default format to "ebook". Most common case for a 1,900+ digital library. Still editable. Two smart defaults (date + format) that eliminate taps for the common path.
- Category segmented control (UF-12): replace  < select> dropdown with segmented control everywhere category appears. 4 options in all contexts: Uncategorized / Fiction / Non-Fiction / FanFiction. Full labels, 11px text, single row (Option D from mockup review). Uncategorized must be available everywhere including add flows — auto-detect at low confidence should default to Uncategorized rather than silently miscategorizing. NNG H5 (error prevention): a confident wrong answer is worse than an honest "I don't know." This gives a natural triage workflow: filter by Uncategorized → categorize manually.
- Rating visibility (UF-03, G3-07): hide StarRating entirely when session status = "In Progress". Don't show disabled/grayed stars — that's a false affordance. Only render when Done or DNF. Remove help text "Rating available after..." — nothing to explain if component isn't there. NNG H5 (don't show controls that can't be used).

#### **==Fix Session 6: Search and Sort Everywhere (2026-04-13)==** 
Findings addressed: UF-21, UF-30, UF-31, G3-08, G5-13
- CollectionPicker search (UF-21, G3-08): search input at top of picker modal, always visible. Client-side filter on collection name. Sort collections alphabetically. "+ New Collection" button at bottom with dashed border — opens CollectionModal (create), returns to picker with new collection pre-selected. NNG H7 (flexibility).
- Author detail sort (UF-30): add SortDropdown with sort icon (↑↓ arrows) to the left of the label text. Options: Series (default), Title A-Z, Recently Added, Year Published. Text-only sort button gave zero context that it was a sort control — icon makes it identifiable.
- Author detail series grouping (UF-31): inline grouping with section headers. Format: "Series: Owl Post · 3 titles" — prefix label makes it explicit that the group is a series. Parallels "Standalone · 20 titles" at the bottom. Section header tappable → links to SeriesDetail (→ arrow as affordance). Within series: sorted by series_number. Within standalone: alphabetical. Selecting any sort option other than "Series" flattens groups into a plain list. Works in both grid and list views — grid shows #N badge on series books.
- Collection detail search (G5-13): search input at top of book list for collections with 15+ books. Below 15, list is scannable without search. Client-side filter on title and author. Positioned below collection info, above sort dropdown.

#### **==Fix Session 7: Settings Consolidation (2026-04-13)==** 
Findings addressed: G1-11, G6-08, G1-07
- Kill SettingsDrawer: move all content into Settings.jsx page at /settings. Delete the drawer component. Bottom nav Settings tab (already routing to /settings) becomes the single entry point.
- Remove gear icon from all page headers (Library, Series, Authors, Collections). Was inconsistent and now redundant. NNG H4 (consistency): one entry point, one destination.
- Settings page layout: single scrollable page with section headers. UnifiedNavBar with "← Library" back.
- Section grouping follows NNG principle: group by user goal, not system domain. Three types of content separated: preferences (set and forget), customization (personalize), actions (do something now). This eliminates the chaos of mixing light switches with fire alarm buttons.
- Section order: (1) Appearance — Books per Row, Card Style, Status Labels (with current values preview + "Edit →"), Rating Labels (same). (2) Reading — Reading Speed with "Used to estimate read times" sublabel. (3) Library Tools — Sync Library, Find Duplicates, Rescan Metadata, Extract Covers (all action buttons with one-line descriptions), Enhanced Metadata toggle. (4) Backups — Automated Backups toggle, Schedule, Backup Location, Retention (with "Edit →"), backup stats summary (last backup, storage, count), Create Backup Now button.
- Visual patterns: preferences show values on the right. Actions show → arrows. Toggles show switches. Three distinct patterns so user never confuses "shows info" with "does something."
- Button colors simplified: primary (teal) for all safe actions. No more 4-color rainbow (G6-09).

#### **==Fix Session 8: Status Label + Voice/Tone (2026-04-13)==** 
Findings addressed: G3-06, G4-04, G6-04, UF-10, G6-13, G5-14
- Canonical default status labels (per VOICE_AND_TONE_v2.md): Unread → "Unread", In Progress → "In Progress", Finished → "Finished", Abandoned → "DNF". DNF is the reading community standard — clear, honest, zero ambiguity, fits chips.
- SettingsDrawer/Settings default fix: dnf initial state should be 'DNF' not 'Abandoned' (currently contradicts useStatusLabels default).
- Apply useStatusLabels everywhere (G6-04): grep every component for hardcoded status display strings → replace with getLabel(). Mechanical fix, no design judgment.
- "Paused indefinitely" wrapping (G3-06): solved by changing factory default to "DNF" (short). Soft guidance in Settings label editor: "Shorter labels fit better in filters and badges." Not a hard limit — user autonomy preserved.
- Microcopy: "Add Story" → "Add to Library" (UF-10). Genre-neutral per voice/tone.
- Microcopy: "Search books..." → "Search your library..." (G6-13). Warm + accurate for mixed-content library.
- Microcopy: "All N books loaded" → remove entirely for collections under 20. For 20+: "That's the full list." No count (already in header). VOICE_AND_TONE: warmer than technical status text.

#### **==Fix Session 9: Mobile-First Polish (2026-04-13)==** 
Findings addressed: G2-14, G5-10, G5-03, G7-01, G7-15 Note: UF-33 (author detail view toggle) already resolved — AuthorDetail has Grid/List toggle.
- Edition delete: remove ✕ from BookDetail entirely (G2-14). Edition badges become display-only. NNG H4 (consistency): every other edit action on BookDetail goes through a modal — inline delete was the one outlier. NNG H5 (error prevention): tiny ✕ with no confirmation on the primary display page is dangerous. Edition management moves to 3-dot menu or edit flow. Simpler to implement than fixing the hover pattern, and makes the whole page consistent: BookDetail is for viewing/navigating, modals are for editing.
- "Long-press or right-click" → "Long-press" (G5-10). Drop "or right-click" — 95% mobile usage. Desktop users discover right-click without instruction.
- Collection cover max-height (G5-03): cap at max-h-[200px] with object-cover. ~35-40% phone viewport. Enough for hero moment, books visible without scrolling. NNG H8 (minimalist design): the books are why you navigated here.
- Add choice page (G7-01, G7-15): keep as page, not bottom sheet. Add UnifiedNavBar with "← Library" back (fixes G7-15). Add subtitle with dynamic book count ("1,942 titles and counting") for context and warmth (fixes G7-01).

#### **==Fix Session 10: Destructive Action Guards (2026-04-13)==** 
Findings addressed: G8-02, G3-13 Note: G3-10 (merge modal confirm) already resolved — BookDetail merge has search → confirm flow with "Merge & Delete" danger button.
- DuplicatesPage merge confirmation (G8-02): inline confirmation within the group card, NOT a separate modal (modal fatigue across 44 groups). "Merge into Selected" button transforms into confirmation row: "Merge [N] titles into '[kept title]'? This can't be undone." Buttons: Cancel (ghost) / Merge & Delete (danger). Cancel returns to normal state. One extra tap per group. NNG H5 (error prevention), H3 (user control).
- Session delete in-app confirmation (G3-13): replace window.confirm() with inline confirmation within the session edit modal. Form content replaced with confirmation view (same pattern as Session 4 unsaved changes guard). Text: "Delete Read #[N]? This can't be undone." Buttons: Cancel (ghost, returns to form) / Delete (danger). Consistent visual language throughout. NNG H4 (consistency).

#### **==Fix Session 3: BookDetail Action Architecture (2026-04-12)==** 
Findings addressed: UF-01, UF-02, UF-05, UF-06, UF-20, UF-24, G2-11, G2-12
- Mark Finished: inline in ReadingStatusCard, not sticky bottom bar or FAB. Paired with "Status ▾" secondary button. Only renders for non-finished/non-DNF books.
- Section edit actions: outline pencil IconButton, right-aligned in section headers. Stroke color `#5c5752` (Weight 2 — present if you look, doesn't pull attention). NOT text "Edit" links.
- Tappable metadata: text links in teal (action-primary), not chips. Author name and series name get teal base color (were hover-only). Collections section items become tappable links.
- Pill grid resolution: status pill → opens ChangeStatusModal. Category pill → opens UnifiedEditModal to Details tab. Read time and rating pills → restyle as flat non-interactive (remove border/bg-surface, use plain text layout) to eliminate false affordance. Solves UF-05, UF-24, G2-11.
- Wishlist "I Got This Book" (G2-12): surface in ReadingStatusCard area for wishlist books so it's above the fold.
- Pencil IconButton targets: Notes section, Reading History header (opens add/edit session), Collections header (opens CollectionPicker). Summary/Tags/Metadata stay in 3-dot → Edit modal (too granular for inline edit).
- Section pencil icon color: `#5c5752`, not a new token. Applied as inline style or arbitrary Tailwind class `text-[#5c5752]`.

#### **==Fix Session 2: Cards, Grids, and Context Menus (2026-04-01)==** 
Findings addressed: G1-02, G2-03, UF-32, G1-03, G4-02
- text-muted = `#918b84` (passes WCAG AA 4.5:1 on bg-surface). Replaces text-caption (`#6e6962`) for metadata labels.
- useGridColumns shared hook for grid column preference. All grid pages use it.
- BookCard variant prop: compact/standard/list. Single prop replaces three booleans.
- BookCard list metadata: read time for all except In Progress and DNF.
- DNF gets warm gray dot (`#7a756e`) + label. No text badge (breaks on rename).
- SeriesCard: 2:3 ratio, 3-layer stack, compact badge, standard text, list abbreviation. Warm fallback gradient.
- showTitleBelow/showAuthorBelow/showSeriesBelow settings removed entirely.
- List thumbnail size: 64x96px (up from 44x66).
- List view status badges: solid chip with opaque bg (bg-bg-base/65).
- Custom status labels: useStatusLabels hook with { labels, getLabel, getStatusOptions } return.
- View preference: per-page localStorage keys (liminal-view-browse, liminal-view-series, liminal-view-author, liminal-view-collections, liminal-view-collection-detail).
- Grid variant: global liminal-grid-variant, toggle in SettingsDrawer under "Grid card style".
- GradientCover title: 1rem (16px) at line-height 1.2, 4-line clamp. Frozen file unlock (font size only).
- CollectionDetail BookListItem deleted, replaced with BookCard variant="list".
- WishlistCard: keep as separate component (sibling to BookCard), compact/standard aware. Compact = cover only. Standard = "why this one" note only.
- CollectionCard: CollectionIcon removed from grid and list.
- Grid/List toggle: inline on ALL pages including Collections (NNG Heuristic #4: consistency).
- Checklist completed opacity: 45% to 75%, section wrapped in bg-surface container.
- Remove mode: select-then-confirm batch pattern with bottom bar. "Select titles to remove" banner copy.
- Long-press context menu: all BookCard list views (Browse, Author, Collection).
- Shared components extracted: MarkFinishedModal.jsx, ChangeStatusModal.jsx, BookContextMenu.jsx.
- SeriesDetail: uses getLabel('Abandoned') for DNF display.
- Backend: finished_count added to /api/series/ list endpoint (was exposed as books_read, SeriesCard reads finished_count).
- DNF filter bug fixed: FilterDrawer and Library statuses arrays changed from 'DNF' to 'Abandoned'. getLabel('Abandoned') handles display. API now receives correct DB value.

#### **==Session 2.2 — 2026-04-01==**
- WishlistCard: keep as separate component (sibling to BookCard), add compact/standard awareness. Compact = cover only. Standard = "why this one" note only (never title/author below).
- CollectionCard: CollectionIcon removed from both grid and list views
- Grid/List toggle: inline on all pages including CollectionsTab and CollectionDetail (NNG Heuristic #4 consistency)
- Per-page view keys: liminal-view-collections, liminal-view-collection-detail added
- Checklist completed opacity: 45% → 75%, completed section wrapped in bg-surface container
- Remove mode: select-then-confirm batch pattern. Bottom bar with live count + Remove button. No more instant single-tap deletion.
- Long-press context menu: enabled on all BookCard list views (Browse, Author, Collection). Opens BookContextMenu with "Mark Finished" / "Update Status".
- Shared components extracted: MarkFinishedModal.jsx, ChangeStatusModal.jsx, BookContextMenu.jsx
- Remove mode message: "Select titles to remove"
- SeriesDetail: uses useStatusLabels getLabel for DNF display

#### ==**Session 2.1 — 2026-04-01**==
- List thumbnail size: 64×96px (up from 44×66, maintains 2:3 ratio)
- List view status badges: solid chip with opaque bg (bg-bg-base/65), not transparent scrim
- Custom status labels: useStatusLabels hook (module-level cache + settingsChanged event listener)
- SeriesCard compact: top-right corner count badge (matches BookCard checkmark position), first author on cover bottom, no external author line below cover
- SeriesCard fallback gradient: warm palette (`#8a6e5e` → `#5e7a6e`), replacing cold indigo (`#667eea` → `#764ba2`)
- BookCard linkTo/onClick props: linkTo defaults to book detail, null renders div; onClick for parent-controlled interaction
- CollectionDetail: BookListItem deleted, replaced with BookCard variant="list" in all modes
- View preference: per-page localStorage keys (liminal-view-browse, liminal-view-series, liminal-view-author)
- Grid variant: global localStorage key (liminal-grid-variant), toggle moved from landing pages to SettingsDrawer
- GradientCover title: 1rem (16px) at line-height 1.2, 4-line clamp (FROZEN FILE UNLOCK — font size only)

#### ==**Fix Session 2:** Contrast Token + Grid Settings + View Variants (2026-04-01) Findings addressed: G1-02, G2-03, UF-32, G1-03, G4-02==
- text-muted token lightened from `#6e6962` to `#918b84`. Passes WCAG AA 4.5:1 on all three backgrounds (bg-base, bg-surface, bg-elevated). text-muted and text-caption stay the same value — splitting them adds complexity for no gain.
- Shared useGridColumns hook (frontend/src/hooks/useGridColumns.js) replaces copy-pasted useState+useEffect+useEffect pattern. Reads grid_columns from API on mount, listens for settingsChanged events. Returns { gridColumns, gridClasses } where gridClasses is a ready-to-use Tailwind string. Applied to Library, WishlistTab, AuthorDetail. CollectionDetail adopts the hook too. HomeTab exempt (curated 3-col layout). SeriesDetail exempt (table layout, no grid yet).
- BookCard gets variant prop: compact (cover only), standard (cover + title + author below), list (horizontal row with 44x66 thumbnail). Compact/standard toggle replaces the three individual showTitleBelow/showAuthorBelow/showSeriesBelow settings toggles. Net settings reduction — fewer knobs, clearer mental model.
- BookCard list metadata line: read time (clock icon + ~Xh) shown for all statuses except In Progress and DNF. In Progress gets teal dot + label + progress bar. DNF gets warm gray dot (`#7a756e`) + "DNF" label — matches the dot+label pattern from In Progress, visible without being alarming. Finished books show read time — "that was a 14-hour read" is useful context. DNF title is NOT dimmed — dimming makes text harder to read, not calmer.
- SeriesCard redesigned: 2:3 aspect ratio (was 1:1 square) to match BookCard. 3-layer stacked card effect with fading borders (4px/8px offset, decreasing border opacity) visually signals "this contains multiple items." Compact mode: series name on gradient + centered count badge (number only). Standard mode: series name + "N books" text on gradient, title + author below cover. List mode: 2:3 thumbnail with 2-letter abbreviation + title/author/stats row.
- SeriesCard abbreviation: first letter of first two significant words, skipping articles (The/A/An). Single-word names take first two letters.
- View toggles (Grid/List, Compact/Standard) now drive BookCard and SeriesCard variant props via localStorage. Toggles show on Browse tab and Series tab. State persists across sessions.
- showTitleBelow, showAuthorBelow, showSeriesBelow settings removed from SettingsDrawer. The compact/standard toggle is the mechanism. Settings API values are left in the database (harmless) but no longer read or displayed.
- Legacy hardcoded colors in BookCard cleaned up: text-white → text-text-primary, text-gray-400 → text-text-secondary, bg-library-accent → bg-action-primary, border-gray-500 → border-border-default, text-gray-500 → text-text-muted.

#### ==**Fix Session 1: Broken Wishlist Conversion (2026-03-31)** - Findings addressed: UF-14, UF-15, UF-16==
- Backend handles TBR→Library conversion atomically. The linkFilesToTitle endpoint detects when the target title has acquisition = 'tbr' and flips it to owned within the same database transaction as the edition creation. The frontend does not orchestrate two separate calls. One transaction boundary, one place for it to fail.
- Idempotent retry via IntegrityError catch. When an edition INSERT hits a UNIQUE constraint (because a previous attempt half-completed), the backend catches IntegrityError, skips the insert, and proceeds with the acquisition flip. Same code path handles fresh conversions and retries. No special cases.
- Startup migration for existing half-states. On every Docker container boot, the backend runs a query that finds titles where acquisition = 'tbr' AND an edition exists, and converts them to owned. First run fixes existing damage; subsequent runs find zero rows. Includes logger.info output visible in Docker logs.
- Raw SQL errors never reach the user. Backend endpoints catch database exceptions and return sanitized error messages. Frontend API layer adds a second safety net that replaces any SQL-sounding error strings with human-readable copy. Belt and suspenders.
- Path B (physical/audiobook) conversion gets toast feedback. The acquire modal's error handling changes from silent console.error to closing the modal and showing a toast: "Something went wrong. Try again?" on failure, "Moved to your library" on success.
- Synthetic results include title_id. The AddPage linkTo flow's synthetic uploadResults and books objects both include title_id so UploadSuccess can build a working "View Story" navigation link.
- 2026-03-29 | FileDropZone: mobile gets compact button + file name preview; desktop keeps drag-and-drop rectangle. Extract to components/ui/. | "Drop files here" on a phone is a lie 2026-03-29 | Manual add (Physical/Audiobook/Web): convert to FormField as-is, no extra polish | Used occasionally, not worth over-investing during conversion 2026-03-29 | Review form fields: convert to FormField as-is | Fields are correct for the workflow 2026-03-29 | Fix title field bug during C7: manual edits on review screen not persisting to book detail | Bug is in the same files, cheaper to fix during conversion than as a separate pass

- 2026-03-29 | All filter drawer filters (Category, Status, Length, Tags, Formats) are actively used. Convert as-is, no removals. | Every filter serves a real browsing use case 2026-03-29 | Fix tag search ordering in C8: sort by relevance/alphabetical when searching, not by instance count | Current count-based ordering buries exact matches under partial matches (e.g. "slow-burn" above "slow-sexual-burn") 2026-03-29 | Sort-by-estimated-time: parked as feature work, not conversion scope | Requires backend sort option, not just frontend conversion 2026-03-29 | BookCard variants: standard/compact/list covers all contexts, no custom overrides needed | Search results, author pages, etc. all work with existing variants 2026-03-29 | Misc file priority: StepIndicator and error/404 pages converted during C8. Settings panel, Import page, Duplicates page are last priority (rarely seen in daily use). | Convert what you see daily first

- 2026-03-29 | Component preview page: in-app route at /dev/components, built before C3. Shows all 9 components with variant/state combinations. Uses real tokens and Tailwind config. | Two groups without it confirmed the pain. Daily-driver screens deserve verified components. 2026-03-29 | FRONTEND_AUDIT_2026.md: commit to /docs/, update counts after each C-group completion | Source-of-truth for conversion scope. Living document beats frozen baseline since "what's left" is more useful than mental subtraction. 2026-03-29 | BottomSheet independence: no action needed. Already a standalone component, just only imported by ThreeDotMenu. No use case for independent usage today. Revisit if one surfaces. | Don't build architecture for hypothetical futures.

- 2026-03-28 | User-facing label stays "Wishlist." File renamed TBRList → WishlistTab.jsx | "Wishlist" is what Marie actually says; TBR was internal-only naming 2026-03-28 | HomeTab error: inline error + retry button, per-section when possible, styled with Liminal voice copy. Not a toast (too ephemeral for landing screen). | NNG: errors must be visible, persistent, actionable. Exact copy deferred to C3 prompt session. 2026-03-28 | Library NavBar: Settings gear in top-right slot. No other icons. | Settings is the least-used action; frees bottom bar real estate. Full nav redesign parked separately. 2026-03-28 | Navigation redesign parked as Open Question. Usage ranking: Library > Search > Add (+) > Collections > Authors > Series. Two bottom bars is a known problem. | Touches every screen — too big for C3 scope 2026-03-28 | Grid/list view preference persists in localStorage until manually changed | Marie switches views fairly often; resetting per session would be annoying 2026-03-28 | Library default grid variant: compact (4-col, covers only). Toggle available, preference persists. | Information density wins for a 1,700+ book library 2026-03-28 | ChipInput: text input on top, chips accumulate below. Standardized everywhere. | Thumb stays near keyboard, eyes track downward naturally 2026-03-28 | Drawer width: one standardized pattern for all drawers (settings, filters, etc.) | Less code, less cognitive overhead. Different content, same container. 2026-03-28 | ChangeCoverModal: fullscreen on mobile, standard on desktop | Matches C2 precedent for complex modals with browsing/uploading 2026-03-28 | Collection cards get distinct visual treatment from book cards | Collections are containers, not titles — they should read differently. Exact styling deferred to C5 prompt session. 2026-03-28 | SmartPaste: cut entirely. No Escape fix needed. | Rarely/never used. Building things you don't use is how we got here. 2026-03-28 | Checklist collection dimming: keep 45% opacity for completed items, adjust visually after warm tokens land | Dimming is the right concept; exact value may need tuning on new background 2026-03-28 | Series and Authors landing page titles: just "Series" / "Authors" — no app name suffix | Clean, functional, matches other page titles 2026-03-28 | Author detail: convert colors/tokens only. Existing content (name, count, bio, book grid) preserved. Author-level filtering parked for later. | Feature work doesn't belong in conversion scope 2026-03-28 | Series numbering: keep "01, 02, 03" format | Supports decimals for prequels/side stories (e.g. 2.5); zero-padded is intentional

- 2026-03-28 | Modal footer is a slot (children), not a template API | 3-action and multi-step modals already exist; enforced props would fight every exception 2026-03-28 | StarRating: one component with readOnly prop | Display and input share the same render tree; split not justified by divergence 2026-03-28 | Form labels: always stacked, constrain input width for short-value fields | NNG mobile best practice; no inline escape hatch, no conditional in FormField 2026-03-28 | Error states (cross-cutting C2-C8): banner at top + red border on offending fields, no per-field message text | NNG "errors on both" pattern; dual signal without vertical expansion 2026-03-28 | Complex modals (scrollable lists, multi-step flows): fullscreen on mobile, standard on desktop | Merge modal sets the precedent; Modal already supports size="fullscreen"

- 2026-03-28 | C1 inline text actions (Edit, Add reason) use plain < button > not < Button > | Ghost variant needs !important overrides to look like text links, which means it's the wrong component 2026-03-28 | BookDetail.jsx lives in components/ not pages/ | Cursor caught the path mismatch, future prompts need correct path

- 2026-03-27 | Teal is primary, full stop. Indigo is dead. Any remaining indigo is a bug. | Warm A tokens already decided this — making it explicit 2026-03-27 | Typography mapping confirmed. text-h2 for page/book titles, text-h4 for section headers, text-body for descriptions/notes, text-body-sm for author/secondary, text-label for form labels, text-caption for timestamps/metadata. No custom sizes. | Validated via HTML mockup on mobile 2026-03-27 | Buttons for on-page actions (save, delete, edit, toggle). Links for navigation (View Series, View Author). Ghost Button for section-header actions that trigger modals. No link-styled Buttons, no Button-styled links. | Clean semantic split 2026-03-27 | Edit affordance: pencil IconButton (small, 36px) right-aligned in every editable section header. 3-dot menu for page-level actions only (delete, merge, change cover, download). No overlap, no text Edit buttons. | NNG: proximity to content + consistent pattern, IconButton avoids visual weight of text buttons 2026-03-27 | Every BookDetail section gets a bg-surface card with border. No mixed treatments, no flowing sections. Consistent card pattern everywhere. | Uniform > varied. No per-section decisions during conversion.

- 2026-03-27 | Hardcoded color strategy: hybrid approach. Script handles mechanical token mapping, flags edge cases for manual review. AI overkill for find-replace; regex + mapping table does the bulk work. | 1,445 instances across 62 files — manual review of all would be brutal 2026-03-27 | Conversion standardizes, not just tokenizes. When styles vary across pages (headings, labels, spacing), collapse to one token rather than creating multiple. Visual changes from standardization are expected, not regressions. Zero hardcoded Tailwind defaults survive conversion. | Kill the snowflakes, don't catalog them 2026-03-27 | Testing cadence: thorough test of converted screens + quick adjacent-screen sanity check per group. Full regression at two milestones — after C3 (before feature work) and after C8 (conversion complete). | Per-group full regression is overkill; blind spot-checking is risky given intentional visual changes 2026-03-27 | Git strategy: one commit per C-group, straight to main. No branches. | Solo dev, Docker rebuild from disk, branches add ceremony with zero benefit 2026-03-27 | Update SKILL.md to remove old color references before C1. tailwind.config.js is the sole color source of truth. Claude Project task, not a Cursor prompt. | Conflicting color guidance in SKILL.md will cause Cursor to produce inconsistent output

- 2026-03-27 | SKILL.md updated: removed old navy/indigo color references, added full typography token mapping, added component inventory, added section pattern and button/link rules. tailwind.config.js is sole color authority. | Conflicting guidance was blocking C1 2026-03-27 | C1 prompt strategy: instruction-based mapping tables, not 257 individual find/replace blocks. Cursor handles systematic instructions better at this scale. | 3,026 line file, 257 color instances, 45 buttons 2026-03-27 | Inline ThreeDotMenu (BookDetail lines 96-214) gets colors converted in C1 but component swap deferred to separate prompt | Avoid scope creep on already large C1 prompt

#### **==Seed entries
(backfilled from ROADMAP, CHANGELOG, and project conversations)==**

2026-03-25 | Design system lives in `components/ui/` subfolder | Separation from feature-specific components 2026-03-25 | Design tokens: Warm A palette (Phase 10.0A) | Swan Song inspired, warm charcoal base, muted teal primary 2026-03-25 | Six new base components: Button, IconButton, Badge, SearchInput, Modal, FormField | Built in 10.0B, all at 0 imports 2026-03-25 | BottomSheet built as standalone component in 10.0B | Currently only used inside ThreeDotMenu 2026-03-25 | Three existing components relocated to ui/: UnifiedNavBar, Toast, CollapsibleSection | Consistency 2026-03-25 | Status label "Abandoned" → "DNF" | Community standard, no shame connotation 2026-03-25 | UI copy avoids "books" — uses "reads," "works," "titles" | Many items were never physical books 2026-03-25 | All transitions use 0.2s ease-out | Breathing, not clicking 2026-03-25 | Mobile-first, 44px minimum touch targets | 95% mobile usage on Android 2026-03-25 | No component library — everything custom | Intentional choice 2026-03-25 | No TypeScript — plain JSX | Intentional choice 2026-03-25 | DB table is `titles`, router is `titles.py`, endpoints still `/api/books/` | Phase 5 rename, backward compat preserved 2026-03-25 | Rating field must always be `Optional[float]`, not `int` | Pydantic v2 gotcha — half-star ratings (4.5) crash with int type 2026-03-25 | BookCard v4: single `variant` prop (standard/compact/list) replaces 3 boolean props | Cleaner API, enforced during 10.0C 2026-03-25 | Grid badges: opaque dark bg (`#1a1918` @ 88%) + white icon | ~7:1 contrast on any gradient 2026-03-25 | DNF badge: pause icon, not text | Text badge breaks when user renames status in settings 2026-03-25 | Wishlist badge: bookmark icon, no dashed border on card | Badge is sufficient indicator 2026-03-25 | In Progress: 4px teal progress bar with opaque dark track | 50% default until real reading data exists 2026-03-25 | List view: cover overlays for Finished/DNF, not text status labels | Overlays are clearer than text at small sizes 2026-03-25 | Killed from BookCard: left-edge color stripe, backdrop-blur, text DNF badge | Stripe unintuitive, blur poor perf, text breaks on rename 2026-03-25 | ThreeDotMenu: desktop dropdown + mobile bottom sheet, extracted to shared component | Unblocks Series/Author/Collection detail pages in 10.0C 2026-03-25 | Warm gradient palette: 10 lanes replacing 8, saturation 18-30% | Vivid Tailwind-stock colors clashed with warm token palette 2026-03-25 | Modal pattern: ✕ always on right, Cancel + Primary footer | Standardized across all 26 modals during 10.0C 2026-03-25 | No "convert as you touch" — systematic file-by-file conversion | Opportunistic conversion is how we got 0% adoption 2026-03-25 | C1-C3 before 10.1, C4-C8 interleaved with feature phases | Covers 90%+ of daily screen usage first 2026-03-25 | Checklist pagination uses version reference pattern | Prevents stale API responses from corrupting state mid-scroll 2026-03-25 | Conversion groups use C1-C8 labels | Simpler than ROADMAP's original "Group 1a/1b/3+4" numbering 2026-03-25 | Vault status tracking: 3 states only — `[ ]` not started, `[-]` in progress, `[x]` done | 5 emoji stages too fiddly for phone use, won't survive a month